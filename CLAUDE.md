# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Core directives

Priority order: **Safety > Correctness > Clarity > Efficiency.**

1. **Secrets and PII hygiene:** never read `.env` or other secret-bearing files (keys, certs, credential stores). Never print, echo, or paste secrets or PII into chat, code, comments, logs, or sample output; reference secrets by env var name and mask or omit PII.
2. **No decorative Unicode:** no emojis or decorative icons in code, comments, or docs. (Semantic characters like the yen sign in game copy are fine.)
3. **DRY:** search for an existing implementation before writing new code; no duplicate implementations.
4. **Single responsibility:** extend a unit when the concern is the same; split when responsibilities diverge. Prefer small single-purpose pipelines behind an orchestrator over nested branching.
5. **Read first:** before editing a file, read it in full; never edit a region you have not seen whole.
6. **Truthful and verified:** state uncertainties explicitly and never misrepresent results. Before reporting complete, run the project's checks (tests / lint / typecheck / build) and show the output. Show evidence, do not assert it. Nothing reaches production without user review; mark work only as "ready for review".
7. **Git safety:** FORBIDDEN: `reset` / `force` / `rebase` / `clean`. ALLOWED: `status` / `log` / `diff`. Commits need explicit permission (the commit step of the sprint workflow below counts once the user approves the sprint's work).
8. **Human air gap on data:** never access or modify data via MCP, cloud shell, CLI, or scripts. Hand any command that could touch data to the user to run. No exceptions.
9. **Stay on stack:** do not add, swap, or deviate from declared dependencies (`package.json` / `pyproject.toml`) or the locked stack below without explicit user approval.
10. **Professional comments:** document what the code does, not the process of writing it. Comment only where the code is not self-documenting.
11. **Clean codebase:** tests live in `tests/` directories, never in the package root. No scratch docs in the repo root. Archive temporary or outdated files rather than deleting them. Clean as you go.
12. **No background processes:** never run long-running or background processes; give the user the exact command to run in their own terminal.
13. **Token discipline:** when investigating (not editing), read narrowly — scope to the relevant file or symbol. Never load data files into context: inspect schema, row counts, and a small sample by command. Filter command and test output to what matters.
14. **No commit co-authorship line:** never add a "Co-Authored-By: Claude" (or any similar tool-attribution) line to a commit message, under any circumstance.
15. **Reuse-first design (DRY at the design level, not just code):** before implementing ANY feature, first write down — in the sprint doc or the plan — (a) what mechanisms are genuinely **new**, and (b) what **already exists that must be reused** (systems, state shapes, sim functions, UI flows). Map the new feature onto existing primitives before writing a line of code; never stand up a parallel mechanism when an existing one covers the concern. The Sprint 08 service-jobs rework happened because a whole second "job" system was built alongside the existing job/labor system instead of reusing it — that is the exact failure this rule exists to prevent.

## Sprint workflow (mandatory)

All development happens in sprints, driven by the roadmap. Sprint docs live in `docs/sprints/sprintXX.md`. For every sprint:

1. **Read** the roadmap and planning docs (and the previous sprint doc, if any).
2. **Design** `sprints/sprintXX.md` from them: goal, definition of done, task breakdown separating Claude-implementable tasks from user-only tasks (purchases, account setup, anything behind the data air gap). **The design MUST open with a reuse analysis (directive 15): an explicit "new mechanisms" vs. "existing mechanisms to reuse" split, mapping every part of the feature onto what already exists before any code is written.**
3. **Implement** all actions in the sprint doc.
4. **Test** new implementations where relevant; run the project checks and show output.
5. **Hygiene:** clean up, update docs (including this file and the sprint doc's checkboxes) so they accurately track project state. Also check `TODO.md` (repo root) — deliberately deferred items that aren't tied to any specific future sprint number, so they don't get lost in sprint-doc archaeology. Add to it when you defer something with no sprint attached; remove from it once something gets actioned.
6. **Commit** (with user approval per the git safety rule).

## What this repository is

**Midnight Garage** — a browser-based, turn-based garage management sim set in 1995 Japan (synthwave pixel art, JDM car culture, hunt-build-sell loop). Solo-dev passion project, ~13-month roadmap to a free itch.io launch.

**Current state: Sprint 12 implemented, ready for review** (`docs/sprints/sprint12.md`; not yet committed — awaiting the maintainer's review per the sprint workflow). Sprint 11 committed & pushed (`ea99335`; Sprints 03-10 through `f44ce0f`: Sprint 10 `2fbdbaf`, Sprint 09 `94ecbea`, Sprint 08 `998e014`, Sprint 07 `8629fe3`, Sprint 06 `1a07510`, Sprint 05 `ffc9be4`, Sprint 04 `da7c2fb`, Sprint 03 `fa6cc79`, Sprint 02 `97af527`, Sprint 01 `832e47b`, Sprint 00 `00b44e2`). Sprint 12 (Component model refactor) is the "14a" component-model migration from `docs/design/repair-replace-progression.md`'s "Option B," implemented per `TODO.md`'s 10→11→12→13→14 sequencing so Sprint 13's equipment/repair-vs-replace economy has one consistent thing per car component to gate. **The core change: `CarInstance`'s old split of a 5-entry `condition` zone map and a 7-entry `buildSheet` slot map (different key sets, no shared identity, two slots — `bodyAero`/`wheelsInterior` — that didn't map onto a single real part) collapses into one unified 8-key `components` map** (`ComponentIdSchema`: engine/forcedInduction/drivetrain/suspension/brakes/wheels/body/interior; each entry `{condition: 0-100, installed: PartInstance | null}`). Ripple was genuinely wide but mechanical: `computeDerivedStats` (loop over `components` instead of a separate condition read + a slot loop — condition-to-stat feed deliberately stays scoped to the same 4 components that fed stats before this sprint, engine/suspension/body/drivetrain, an explicit and tested anti-balance-change guard, since brakes/wheels/forcedInduction never had a condition-to-stat pathway even in the old model), `generateAuctionCarInstance`/`resolveHandoverCondition` (auction generation + the lemon rule), the job/labor core (`jobs.ts`'s `NewJobSpec`/`Job` collapse `zone?`/`slot?` into one required `componentId`), all 5 harness bots + service-grinder (repair-loop targets renamed, restricted to the same 5 stat-feeding components as before — unchanged bot behavior, since repairing an inert component would be pointless), and `CarDetailScreen.vue` (the old two-section "Condition"/"Build sheet" layout collapsed into one 8-row Components list). Content ripple: `Part.slot` → `Part.componentId` across `parts.json` (20 parts), `HiddenIssue.zone`/`CarModel.hiddenIssueWeights[].zone` → `componentId` across `hidden-issues.json`/`cars.json` (values unchanged — no hidden-issue content targets the 3 new components yet), and the old `wheelsInterior` slot's 3 parts hand-reclassified by name (`enkai-mesh-15`/`vulk-ve37` → `wheels`, `zashiki-bucket-seat` → `interior`) — narrower than the design doc's own summary, which assumed `interior` ended up with nothing installed post-migration. **Two decisions were revised mid-flow at the maintainer's explicit direction, before implementation began:** (1) **no save migration** — "STOP worrying about existing saves... they don't exist. Just nuke." `SAVE_VERSION` still bumps 4→5 (a real `GameState`-nested shape change), but ships with no `MIGRATIONS[4]` transform; a pre-v5 save containing a car now fails `decodeSave` cleanly (its old `condition`/`buildSheet` shape no longer matches `CarInstanceSchema`), caught by the store's *already-existing* `hydrate()`/`importSaveCode()` fallback (start a fresh career) — nothing new needed building for it, only testing. (2) **correlated per-car condition rolls** — "not true random. not realistic if for example engine rolls 100 and transmission rolls 1. components need to have a relationship." `generateAuctionCarInstance` no longer rolls each component's condition independently; one baseline is rolled per car (`CAR_CONDITION_BASE_MIN`/`MAX`, keeping the old 30-90 range) and each of the 8 components jitters around it (`CAR_CONDITION_JITTER`, new constants in `constants.ts`) — a car still varies component-to-component but can no longer roll a 100 and a 1 side by side; applies to all 8 components (not just the 3 new ones), since the correlation gap was real for the original 5 too. Also split the old `install-wheels-interior` service-job type's flavor pool — which mixed wheels-themed and interior-themed lines under one slot — into separate `install-wheels`/`install-interior` types (13 job types total, up from 12) once wheels and interior became real, separate components; a genuine instance of the exact flavor/work mismatch class Sprint 11's job-type + flavor-pool model exists to prevent. Both `advanceDay.test.ts` golden-master hashes re-pinned (`27aa1230`/`a7dc17af`). 301 tests (was 296) — two coverage gaps (the wheels/interior part reclassification and the new `install-wheels`/`install-interior` types had no dedicated regression) surfaced in self-review after the fact and were closed same-session. **Two follow-ups intentionally left open, tracked in `TODO.md`:** a manual browser check of `CarDetailScreen.vue`'s new unified Components list (never visually verified — `pnpm dev` is the maintainer's to run), and a `pnpm balance:run` pass to check the correlated-condition-roll change at population scale (unit-verified, not harness-verified). Sprint 13 (equipment catalog, purchase actions, gated repair) builds directly on this component model; explicitly out of scope here. Sprint 11 (Instant actions, a real content-authoring system, and round-2 playtest fixes) is the second playtest-driven sprint, combining nine round-2 notes with the already-planned "instant action resolution" architecture work. **The core architectural change: every player action resolves the instant it's clicked, not queued for End Day.** The old `pending: DayActions` / `commitDay()` client-side queue is gone entirely — repair, install, inspect, bid, buyout, buy-part, accept-service-job, sell-walk-in, and list-for-sale each got a pure instant resolver (`resolveJobLabor`/`applyAvailableLaborToJob` in `jobs.ts`, `resolveInspectLot` in `auctions.ts`, `resolveBidInstant`/`resolveBuyoutInstant` in `bidding.ts`, `resolveSellViaWalkIn`/`resolveListForSale` in `selling.ts`, `resolveAcceptServiceJob` in `serviceJobs.ts`, `resolveBuyPart` in a new `parts.ts`) that the store calls directly on click, mirroring the `moveCar`/`buyBay`/`resolveServiceJob` pattern Sprint 08/09 established. `advanceDay` shrank to a pure day-boundary tick (labor reset, weekly rent/market-heat drift, catalog refresh/expiry, the service-job deadline backstop) that still resolves bots' queued `DayActions` batches through the *same* instant resolvers, one call per queued action — one resolution path, two triggers, not two implementations. A new **live daily labor counter** (`laborSlotsSpentToday` on GameState, additive, `SAVE_VERSION` 3→4) lets repair/install clicks spend against today's remaining budget one click at a time; a repeat click on the same car+zone/slot continues the existing job (`findOrCreateJob`'s car+kind+zone/slot-derived id) instead of duplicating it. Bots keep their own `job-${day}-${i}` id scheme unchanged (predicted within one decision tick) — the two id schemes never need to agree, since a GameState is only ever a bot's or only ever a player's. **Round-2 playtest fixes, all landing alongside the architecture work:** (1) the "feeding frenzy" interest badge was firing on ~30-50% of auctions because Sprint 10 shipped its contender-count thresholds unrecalibrated for the new field's own average size (~6.2) — re-measured via a 2,000-sample empirical probe and moved to `quiet:0/warm:<=3/hot:<=11/frenzy:>11`, landing frenzy at ~7%, matching the economic target; (2) inspecting an auction lot no longer costs a labor slot (just the existing cash travel fee) — labor was the tightest resource in the game and gating a look-before-you-buy action behind it was pure friction; (3) a new `swapCars(state, serviceCarId, parkingCarId)` atomically exchanges a service-bay car and a parking car, fixing a real soft-lock (shop exactly at capacity — zero slack either direction, so a direct `moveCar` can never succeed) with a "swap with…" picker on `GarageScreen.vue`; (4) the sell-side buyer pool is now gated by the same explicit-`tierPreferences` rule Sprint 10 already applied to auction bidding — `sellViaWalkIn`/`listPubliclyAskingPrice`/`bestFitBuyer` all reuse `bidding.ts`'s exported `interestedBuyers`, fixing both "a collector makes walk-in offers on a shitbox" and the backwards walk-in-always-beats-listing pricing (averaging in uninterested buyers was structurally dragging the "market price" listing channel below what a single well-matched buyer would pay, inverting GDD 6.3's intended fast/cheap vs. slow/valuable relationship). **A real content-authoring system replaces Sprint 10's flavor-text patch**: after Sprint 10 shipped one car-agnostic description per fixed template, the maintainer found a *new* instance of the same bug class (a "brakes" flavor line attached to a `suspension`-zone job) and asked for a structural fix, not another one-line patch — `ServiceJobTemplateSchema` (8 fixed `{customerName, description, work, payoutYen, baseReputation}` entries) became `ServiceJobTypeSchema` (12 entries — one per repair zone × 5 and install slot × 7, up from 8 incomplete ones — each carrying a `flavorPool: string[]` and a `payoutRangeYen` tuple), with `customerName` split into its own independent `SERVICE_JOB_CUSTOMER_NAMES` pool. `generateServiceJobOffers` now composes an offer from three independent random picks (type, name, flavor line) instead of copying a template wholesale — a flavor line can never be paired with a `work` it wasn't written for, because it only ever lives in its own type's pool; guarded by a new `integrity.test.ts` regression asserting no repair-zone flavor line names a different zone (or "brakes"). Both `advanceDay.test.ts` golden-master hashes re-pinned (the function's shape changed completely). 295 tests (was 251). Staff (#9 from the original playtest list) and the parts-market cart/checkout overhaul (#7) stay out of scope, tracked as their own future sprints in `TODO.md`. Sprint 10 (Auction realism, pacing, and feedback) is the first sprint driven by the maintainer's own **direct playtest notes** rather than the roadmap sequence — a new, standing workflow: play, dump raw feedback, turn it into the next sprint(s). Eleven playtest items came in; this sprint fixes the eight addressable without an architecture or data-model change (items 7/8/9 — repair/replace, parts-market depth, instant same-day actions — are their own future sprints, tracked in `TODO.md`). **Day 1 now has something to do**: a shared `sim/catalogs.ts` (`refreshCatalogs`) extracted from `advanceDay`'s weekly boundary is called once more by `createInitialGameState`, so a brand-new career already has auction lots and service-job offers instead of forcing a "skip the first week" ritual. **Service-job flavor text no longer contradicts the actual car** (`serviceJobs.json`'s 8 descriptions rewritten car-agnostic; guarded by a new `naming.test.ts` regression test). **The auction rework is the bulk of the sprint** — five root causes behind "auction feels rigged, only winnable via buyout": (1) the rival field was hard-tied 1:1 to the 5 named buyer archetypes (always all 5, always "frenzy") — replaced with a **variable, bell-distributed field of anonymous bidders** (`bidding.ts`'s `buildRivalField`, seeded per-lot-id so the shown read and real resolution never disagree; size ~`bellNormal(AUCTION_FIELD_BASE + AUCTION_FIELD_PER_INTEREST × interestBreadth, AUCTION_FIELD_SIZE_SD)`, a new Irwin-Hall `bellNormal` helper in `rng.ts`); (2) rival bids reused `valuateCarForBuyer` (full retail — the same number a customer pays for a *finished* car), so there was no buy-low margin — split off a new `auctionBidValueFor = valuateCarForBuyer × AUCTION_BIDDER_DISCIPLINE (0.70)` in `valuation.ts`, leaving sell-side valuation untouched; (3) the tier-interest gate silently fell back to a nonzero default for unlisted tiers (every buyer wanted every car) — now requires an explicit `tierPreferences` entry, so e.g. a legend-only collector never bids on a shitbox; (4) the shown "interest" estimate centered on the second-highest (clearing) bid instead of the top bid needed to actually win — re-centered, and `AuctionScreen.vue` relabeled from "likely sells X-Y" to "bid X-Y to win"; (5) rival bids had no ceiling and could exceed instant-buyout price — every rival bid now capped at `buyoutPriceYen`. Calibrated via Monte Carlo against the real 5-buyer roster to the maintainer's explicit target (average field 3-9 bidders, win-price a bell: steal/frenzy ~5-10% each, mid the majority) — verified by a new statistical regression in `bidding.test.ts`/`lotInterest.test.ts` across many seeded synthetic lots. A new pure `sim/calendar.ts` (`currentGameYear = 1995 + 2 × reputationTierIndex`, per GDD §2.2) threads a `currentYear` parameter through `auctions.ts` so no auction car's model or rolled year can predate or postdate the in-game calendar (`reputationTier` is never mutated yet, so this is a no-op at `unknown` today but the wiring is real and tested). Completing a service job now shows an immediate `JobCompleteModal.vue` (mirrors `DayReport.vue`'s overlay/dismiss shape) with a flavor line, payout, reputation delta, and — for installs — part cost and profit; `resolveServiceJob` in `serviceJobs.ts` gained the optional `partCostYen`/`profitYen`/`daysSpent` log fields (no `SAVE_VERSION` bump — `DayLog` isn't persisted). Both `advanceDay.test.ts` golden-master hashes re-pinned (day-1 seeding + the field rework change RNG consumption). The harness (`exportCareers.ts`, `runCareer.ts`, `report.py`) gained a per-won-auction win-price bucket metric and an average-field-size sample to verify the bell calibration against real bot play, not just the unit-level formula. `pnpm balance:run` itself was found broken end-to-end on a pre-existing, unrelated bug (`@midnight-garage/content`'s `package.json` `exports` points straight at `src/index.ts`, which plain Node can't resolve/execute; predates Sprint 10) — fixed same-session at the maintainer's request: `tsconfig.cli.json` now compiles `content/src/index.ts` as an explicit root and a new `scripts/fixContentRequires.cjs` post-build step rewrites the compiled bare-specifier `require`s to point at it. A real full run (600,000 career rows, 126,093 auction-win rows, 24,000 field-size rows) now completes and all 4 gated balance invariants pass. **Real-data finding, not yet retuned:** the win-price bell came back STEAL 8.2% / MID 91.8% / FRENZY 0.0% (target 5-10% each on the tails) — FRENZY essentially never fires in real bot play even though the unit-level Monte Carlo predicted ~8%; average field size (6.2) is on target. Logged as a follow-up tuning item in `TODO.md`, not addressed in this sprint. 251 tests (was 231). Sprint 09 (Facilities & bays) makes the shop physical: GameState gains `serviceBayCount` / `parkingBayCount` / `serviceBayCarIds` (one id list — everything not listed is implicitly parked; save `SAVE_VERSION` → 3, additive). **Labor only reaches a job whose car is currently in a service bay** (`advanceDay`'s labor step gates on `serviceBayCarIds`, logging `job-blocked`/`not-in-service-bay` otherwise) — owned-car builds and customer jobs both. **Parking gates delivery, never bidding**: a bid/buyout/service-accept is always allowed to queue; at resolution a car with nowhere to arrive falls through with **no money spent** (`acquisition-blocked`/`no-parking`) — a won auction bid forfeits to the rivals since they already resolved, a buyout/accept just leaves the lot/offer available. **Moves and bay purchases are instant and free for the player** — the `resolveServiceJob` pattern generalized: pure sim cores (`applyMoves`/`moveCar`, `applyBayPurchase(s)` in `sim/facilities.ts`) the store calls directly on click (`moveCar`/`buyBay`, no queue, no cost beyond the bay price), with the *same* cores resolving bots' queued `moveCars`/`buyBays` DayActions at a new advanceDay step 0 — one resolution path, two triggers, matching the completed-job precedent. Bay prices/counts live in content (`facilities.json` via `FacilitiesSchema`/`FACILITIES`), carried on `SimContext.facilities`; a car leaving the shop (sold, listed, or a resolved service job) always releases its bay slot. All five bots (not just Service Grinder) needed a bay-aware move step — a shared `sim/bots/bayHelpers.ts` (`serviceBayBudget`/`claimServiceBay`) is used by Flipper/Cautious Restorer/Balanced Player/Random Strategy's existing repair loops; Service Grinder additionally frees its bay when a job's work is done. `GarageScreen.vue` reorganized into **Service bays** (slots) / **Parking** (the waiting line, owned + badged customer cars) / **Facilities** (counts, next-bay price, instant buy) — the old flat car-grid is gone. `CarDetailScreen.vue` shows a bay-status row with a move toggle; `AuctionScreen.vue`/`ServiceJobsScreen.vue` show a non-blocking "parking full" warning. 231 tests (was 195). *`docs/design/facilities-bays.md`'s design is now fully implemented — no longer just a forward-looking doc.* Sprint 08 (Service jobs — the Act 1 floor): accepting a customer job **brings their real car physically into the shop** (an `activeServiceJobs[].car` `CarInstance`, never owned) and the player does the **actual work through the existing job/labor/parts system** — repair jobs mean queueing a `repair-zone` job and spending labor; install jobs mean buying a part of the required slot at the market and queueing an `install-part` job on that car. Resolution is **immediate and player-triggered — NOT an End-Day action**: clicking "Complete Job" runs the pure `resolveServiceJob(state, jobId, context)` that instant — work done (`isServiceWorkDone`) → fixed payout + reputation (scaled by the installed part's grade for installs, so a pricier part trades profit for reputation); work not done → the job **fails** (no pay, a `reputationForFailure` reputation penalty, clamped at 0). Either way the car leaves; it's never owned. **Every accepted job has a deadline** (`dueOnDay = accept + SERVICE_JOB_DEADLINE_DAYS`); `advanceDay`'s only service-completion role is a **deadline backstop** that runs the *same* `resolveServiceJob` on overdue jobs (pay if done, fail if not) — one resolution path, two triggers (the click and the deadline). No auto-completion on End Day, no part-choice at acceptance. Key code: `serviceJob.ts` (`work` discriminated union `repair`/zone \| `install`/slot; `car: CarInstance`; `dueOnDay`) + `serviceJobs.json` (8 templates), `resolveServiceJob`/`isServiceWorkDone`/`reputationForFailure` in `sim/serviceJobs.ts`, `advanceDay` accept→shop (stamps `dueOnDay`) + deadline backstop + weekly offer refresh (**no bay caps** — bays are the Facilities sprint, `docs/design/facilities-bays.md`), `completeJob` applies to a car in `ownedCars` **or** `activeServiceJobs[].car`, the `repairLaborSlotsFor`/`INSTALL_LABOR_SLOTS` labor helpers moved to `sim/constants.ts` (game re-exports), an accept-only `/jobs` board (deadline countdowns) + a customer-job banner and immediate "Complete Job"/"Give Up Job" button on the car-detail screen, and a Service Grinder harness bot (accepts repairs, works the cars, lets the deadline hand them back). Reputation is scaffolded (`reputationPoints` on GameState). **Save `SAVE_VERSION` → 2** (additive; v1 saves still load via default-fill). *The first pass of this sprint modelled jobs as abstract auto-completing contracts with no real car and no player work — the maintainer flagged that as fundamentally wrong and it was rebuilt end-to-end into the model above; a follow-up pass moved resolution off End Day to an immediate click and added per-job deadlines with failure penalties.* 195 tests. Two future systems remain fully designed but not yet built, in `docs/design/`: `skill-progression.md` and `repair-replace-progression.md` (equipment-gated repair vs. replace, committed to a unified per-component condition model — Option B, ~Sprint 14) — `facilities-bays.md`'s design is Sprint 09, above. Sprint 07 (Persistence & the durable loop) makes a career survive a refresh: Dexie/IndexedDB **autosave** (a store watcher persists every `gameState` mutation), a pure versioned **save codec** (`packages/game/src/save/saveCodec.ts` — `SAVE_VERSION` 1, base64 `MGSAVE1.` codes, a `MIGRATIONS` chain seam, golden-save test) split from a thin IndexedDB-guarded Dexie wrapper (`saveDb.ts`, no-op without IndexedDB so tests need no fake-indexeddb), a copy-paste **save code** export/import (`SaveMenu.vue`, R2 mitigation + `navigator.storage.persist()`), and an **end-of-day report** modal (`DayReport.vue`). `main.ts` hydrates the save before mount. **Save law now live**: any future `GameState` schema change bumps `SAVE_VERSION` + adds a migration + updates the golden-save test, same PR. `dexie` (locked-stack) installed. An **external review (2026-07)** is captured in `docs/reviews/external-review-2026-07.md`: findings 3 (random default career seed) and 5b (a second golden master over the auction-win→sale path) are addressed; the rest are tracked in `TODO.md` with deadlines — the highest being **wire the balance harness into CI before Phase 5** (revisiting the Sprint-03 deferral) and **leash the buyout premium** with harness telemetry before the Fun Gate. 173 tests. Then **service jobs** (the agreed next focus). Sprint 06 (Auction & market screens) closes the buy->build->sell loop: an auction screen (`/auctions`) lists the weekly catalog by tier with inspect + max-bid controls (resolved via the existing second-price `resolveAuction`), the car-detail screen gained a sell flow (walk-in offer or public listing), and a lean parts market (`/parts`) lets the player buy catalog parts to install. New sim mechanics this sprint: `buyParts` and `buyoutLots` (both additive `DayActions` fields + `advanceDay` resolution + log entries; golden-master unchanged). A post-review pass added the **auction Interest meter** (`computeLotInterest` — a fuzzy rival-demand read: level + expected-clearing range + contender count, with a `precision` hook for a future "auction scout" staff trait) and **instant buyout** (pay 1.1x book to win a lot guaranteed), fixing bid "blindness"; a proposed *timed/real-time* bidding war was **rejected** for conflicting with the no-reflex-input pillar. The store's pending plan generalized from `pendingJobs` to a full `pending: DayActions`. A shared `emptyDayActions()` was added to `sim/actions.ts` (bots use it now). 160 tests. **Next focus (user-directed): interactive service/walk-in jobs** — the Act 1 early-game floor + tutorial vehicle (see `TODO.md`); its sequencing vs. Sprint 07 persistence is an open user call. Still in-memory only — persistence (Dexie + export/import + the save law) is Sprint 07, which also retires the dev-grant crutch. Real acquisition works now; dev grants remain for convenience. Detail in `docs/sprints/sprint06.md`. **The economy is now playable end to end and assessable by feel** — the open "too simplified" concern (see `TODO.md`) finally has a surface to judge against; Sprint 08 is the Fun Gate. Sprint 04 stood up the Vue app shell and the state bridge: two Pinia stores in `packages/game/src/stores/` — `useGameStore` (wraps a real `GameState` + `SimContext`, exposes `newGame`/`endDay` calling `advanceDay`, the object Dexie will persist in Sprint 07) and `useUiStore` (ephemeral view flags) — a `vue-router` shell in `createMemoryHistory()` mode (Garage + Spike routes, lazy-loaded so Pixi stays out of the garage chunk), provisional synthwave design tokens + a wired-but-file-later OFL pixel font, and a dev-only console (give-cash / warp-days) that is tree-shaken out of the production bundle. The DoD is live: **End Day advances the real sim in the browser** — cash drains to rent, catalogs refresh, all in a live event log, entirely from `advanceDay` (empty `DayActions` this sprint; real player actions are Sprints 05-06). Sim gained `newGame.ts` (`createInitialGameState`, moved out of the bots module; `runCareer` imports it). Still in-memory only — persistence is Sprint 07. Detail in `docs/sprints/sprint04.md`. The economy is now playable end-to-end in the sim: auction generation, second-price sealed-bid resolution with per-bidder personality noise, shared buyer valuation, and two sell channels, all wired into `advanceDay` (which gained a 4th `SimContext` parameter carrying the static content catalogs). Five bot strategies (`packages/sim/src/bots/`) — Flipper, Cautious Restorer, Balanced Player, Random (a control added at user request), Passive Grinder — play 1,000 seeded 100-day careers each via `pnpm balance:run`; `python -m balance.cli report`/`check` render a markdown report and check 5 invariants (all passing on the real 500,000-row run). **CI wiring is deliberately deferred by user decision** — the harness runs locally, by hand, not on every push (tracked in `TODO.md`, no sprint number attached). This sprint also fixed a real formula bug (buyer valuation was capped below book value, making profit structurally impossible) and corrected `docs/economy-v0.md`'s starting cash (¥1.2M → ¥1.5M — the original left zero operating margin against 100 days of rent). Cautious Restorer's day100 result is honestly negative — a documented, un-gated finding, not a hidden bug. The Random bot's bidding was also caught and fixed mid-review: its "restore" archetype had copied Cautious Restorer's 1.1x bid premium without also copying the reason (Cautious Restorer earns that premium by always inspecting first) — an unearned premium that was winning 78% of its auctions purely by overpaying. Bid size is now uniform across all three Random archetypes. **User's standing review note (2026-07-08, tracked in `TODO.md`): the economy simulation is still considered too simplified** — the framework is good enough to build on, but don't treat Sprint 03's invariants passing as proof the economy itself is right. Full detail, including every implementation-time finding, is in `docs/sprints/sprint03.md` — don't duplicate it here. Sprints 00-02 built the monorepo/CI/art-spike, the full content data model + Naming Layer, and `advanceDay`'s labor/job/rent core, respectively — see their own sprint docs for detail. **Cloudflare Pages deploy is deferred by user decision** — the CI deploy step self-skips until the secrets exist; revisit before Sprint 8 (Fun Gate). Sprint docs live in `docs/sprints/`. Remaining user tasks: Cloudflare setup, Aseprite, trademark search (also tracked in `TODO.md`). The repo holds three authoritative planning documents:

- `docs/design/midnight-garage-gdd.md` — Game Design Document (v0.5). Vision, mechanics, economy, art direction, tech stack. The GDD feature set is **frozen for v1.0**; new ideas go to `IDEAS.md` (repo root, tracked), not into the GDD. `IDEAS.md` is the post-launch parking lot — anything there is out of v1.0 scope by definition. Note: it records a maintainer-sanctioned *post-launch* exception to two hard design rules below (an optional, zero-weight driving minigame) — so don't flag that idea as a rules violation; it's an accepted, deliberately-deferred opt-in, not an oversight.
- `docs/design/midnight-garage-roadmap.md` — Sprint-by-sprint plan (P0–P7), risk register, engineering standards, content pipeline.
- `docs/design/midnight-garage-roster.md` — Full car roster with scope tiers (PoC-10 → Go-Live ~48 → expansion packs). **Contains a secret easter egg ("The Zero Legend") — never surface it in public-facing text, devlogs, or marketing copy.**

When design questions arise, these documents are the source of truth. Don't invent mechanics or cars — check the docs, and treat conflicts between them as something to flag to the user (GDD is canonical for mechanics, roster for car scope).

## Locked technical decisions (GDD §13, roadmap §5)

The stack is decided; do not re-litigate it when scaffolding begins:

- TypeScript (strict) • Vue 3 Composition API • Pinia • Vue Router (memory-history mode; added Sprint 04) • Vite • PixiJS v8 (canvas islands only) • Dexie.js/IndexedDB saves • Zod-validated JSON content • Howler.js audio • Vitest + @vue/test-utils + happy-dom for component tests (the latter two added Sprint 04) • static deploy (Cloudflare Pages / itch.io). Python (polars) for the balance harness. pnpm monorepo.

Planned layout: `packages/sim` (pure TS sim core), `packages/game` (Vue app), `packages/content` (JSON + Zod schemas), `tools/balance` (Python CLI).

Engineering laws that all future code must obey:

1. **Boundary law:** `packages/sim` never imports Vue/Pixi/DOM/Dexie/`@midnight-garage/game` — enforced via ESLint import rules. It does depend on `@midnight-garage/content` (pure data + Zod), never the reverse — that's the one dependency the boundary law permits. Sim contract: `advanceDay(state, queuedActions, seed) → newState + eventLog`, fully deterministic (seeded PRNG; no `Date.now()`/`Math.random()` in sim) — implemented in `packages/sim/src/advanceDay.ts` from Sprint 02 onward.
2. **Content law:** any designer-tunable number lives in JSON under `packages/content`, not in code.
3. **Naming Layer (licensing):** every vehicle separates real `spec` data from swappable `display_name`/`brand` strings; a single config flag flips the whole game to parody names, with a CI test asserting no real-brand string leaks when flagged. Built from the first schema onward. Parts brands are parody-only from day one.
4. **Save law:** every save-schema change = Dexie version bump + migration + golden-save test in the same PR. Also call `navigator.storage.persist()` and keep the export-save-string flow prominent (Safari ITP can evict IndexedDB).
5. **Test law:** golden-master seed tests in CI; every fixed sim bug gets a regression test; balance invariants (bot careers → CSV → polars report, `pnpm balance:run` then `python -m balance.cli check`) exist and pass as of Sprint 03, but run locally on demand only — CI wiring is deliberately deferred by user decision (see `docs/sprints/sprint03.md`).

## Hard design rules (accessibility & scope)

- **No reflex-based input anywhere** — no QTEs, no timing bars. Everything is decision-paced.
- No real-time waiting, energy systems, or monetized timers. Turn-based days: nothing happens while the browser is closed.
- No driving gameplay — events resolve via pre-run decisions + animated resolution.
- Currency is era-authentic yen (¥), everywhere.

## Commands

Run from the repo root (pnpm workspace):

- `pnpm install` — install all workspace dependencies
- `pnpm typecheck` — `tsc --noEmit` in sim/content, `vue-tsc --noEmit` in game
- `pnpm lint` — ESLint (includes the sim boundary-law rule)
- `pnpm format` / `pnpm format:fix` — Prettier check / write
- `pnpm test` — Vitest across all workspace projects; single project: `pnpm test --project sim`; single file: `pnpm test packages/sim/tests/rng.test.ts`
- `pnpm build` — production build of `packages/game` (Vite)
- `pnpm dev` — game dev server (long-running: give this to the user to run, never run it yourself)
- `pnpm balance:run` — plays all 5 bot strategies through 1,000 seeded 100-day careers each and writes `tools/balance/data/careers.csv` + `careers.manifest.json` (plus, since Sprint 10, `auctionWins.csv`/`auctionFieldSizes.csv` for the auction-calibration report section). Builds the CLI via a dedicated `packages/sim/tsconfig.cli.json` (CommonJS, run with plain `node`) first, then two post-build fixups: `scripts/mark-commonjs.cjs` (marks the dist subtree CommonJS) and `scripts/fixContentRequires.cjs` (rewrites compiled `require("@midnight-garage/content")` calls to the dist-local relative path — `@midnight-garage/content`'s `package.json` `exports` points at raw `src/index.ts`, which plain Node can't resolve, so `tsconfig.cli.json` compiles it as an explicit root and this script points the requires at the result). **Not wired into CI** — run it locally whenever you want fresh numbers.
- `python -m balance.cli report --data-dir tools/balance/data --out tools/balance/report.md` — renders the markdown balance report (median/p10/p90 cash + car count at day 25/40/70/100 per strategy). Requires `pip install -e tools/balance` once (polars is the only dependency).
- `python -m balance.cli check --data-dir tools/balance/data` — checks the 5 balance invariants against the CSV; exits non-zero on any failure.

CI (`.github/workflows/ci.yml`) runs typecheck, lint, format, test, build on every push/PR and deploys `main` to Cloudflare Pages once the secrets exist. It does **not** run the balance harness — that's a deliberate, user-requested deferral (see `docs/sprints/sprint03.md` and `TODO.md`), not an oversight; don't add it without being asked.

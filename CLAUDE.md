# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Core directives

Priority order: **Safety > Correctness > Clarity > Efficiency.**

1. **Secrets and PII hygiene:** never read `.env` or other secret-bearing files (keys, certs, credential stores). Never print, echo, or paste secrets or PII into chat, code, comments, logs, or sample output; reference secrets by env var name and mask or omit PII.
2. **No decorative Unicode:** no emojis or decorative icons in code, comments, or docs. (Semantic characters like the yen sign in game copy are fine.)
3. **DRY:** search for an existing implementation before writing new code; no duplicate implementations.
4. **Single responsibility:** extend a unit when the concern is the same; split when responsibilities diverge. Prefer small single-purpose pipelines behind an orchestrator over nested branching.
5. **Read first:** before editing a file, read it in full; never edit a region you have not seen whole.
6. **Truthful and verified:** state uncertainties explicitly and never misrepresent results. Before reporting complete, run the project's checks (tests / lint / typecheck / build) and show the output. Show evidence, do not assert it. Nothing reaches production without user review; mark work only as "ready for review".
7. **Git safety:** FORBIDDEN: `reset` / `force` / `rebase` / `clean`. ALLOWED: `status` / `log` / `diff`. Commits need explicit permission (the commit step of the sprint workflow below counts once the user approves the sprint's work).
8. **Human air gap on data:** never access or modify data via MCP, cloud shell, CLI, or scripts. Hand any command that could touch data to the user to run. No exceptions.
9. **Stay on stack:** do not add, swap, or deviate from declared dependencies (`package.json` / `pyproject.toml`) or the locked stack below without explicit user approval.
10. **Professional comments:** document what the code does, not the process of writing it. Comment only where the code is not self-documenting.
11. **Clean codebase:** tests live in `tests/` directories, never in the package root. No scratch docs in the repo root. Archive temporary or outdated files rather than deleting them. Clean as you go.
12. **No background processes:** never run long-running or background processes; give the user the exact command to run in their own terminal.
13. **Token discipline:** when investigating (not editing), read narrowly - scope to the relevant file or symbol. Never load data files into context: inspect schema, row counts, and a small sample by command. Filter command and test output to what matters.
14. **No commit co-authorship line:** never add a "Co-Authored-By: Claude" (or any similar tool-attribution) line to a commit message, under any circumstance.
15. **No em dashes, ever (CRITICAL, maintainer directive 2026-07-11):** the em dash character (U+2014) is banned everywhere: game UI copy, content JSON strings, code, comments, tests, docs, commit messages, and chat replies. Use a comma, colon, semicolon, parentheses, or a plain hyphen instead. The repo was purged of all em dashes on 2026-07-11; a guard test (Sprint 25) keeps them out of `packages/` permanently.
16. **Reuse-first design (DRY at the design level, not just code):** before implementing ANY feature, first write down - in the sprint doc or the plan - (a) what mechanisms are genuinely **new**, and (b) what **already exists that must be reused** (systems, state shapes, sim functions, UI flows). Map the new feature onto existing primitives before writing a line of code; never stand up a parallel mechanism when an existing one covers the concern. The Sprint 08 service-jobs rework happened because a whole second "job" system was built alongside the existing job/labor system instead of reusing it - that is the exact failure this rule exists to prevent.
17. **A failing test is a diagnosis, not an obstacle:** when a test fails after an implementation change, determine WHY before touching it. Two cases only: (a) the test now asserts stale/wrong behavior (the implementation intentionally changed what's correct) - update it to assert the new correct behavior, or (b) the test caught a real regression - fix the code, not the test. Never edit, loosen, or delete a test merely to make it pass without first establishing which case applies; state which case it was when reporting the fix.
18. **British English everywhere (maintainer directive 2026-07-14):** all player-facing copy, docs, comments, commit messages, and chat replies use British spelling (labour, tyres, colour, -ise). Existing code identifiers keep their current spelling (e.g. `laborSlotsSpentToday` - renaming persisted save-schema field names means a migration for zero player value) unless the maintainer explicitly orders a rename; player-visible strings always use British forms. A spelling guard test (Sprint 63) enforces the copy surface.
19. **No save backwards compatibility before launch (CRITICAL, maintainer directive 2026-07-15):** there are no players and there are no old saves, so do not spend a single line protecting them. **This supersedes engineering law 4's migration and golden-save-test requirements for the whole of pre-launch development.** A schema change needs a Dexie version bump (mechanically required for the DB to open) and nothing else. FORBIDDEN until launch: hand-written data migrations that reshape old records; golden-save tests that pin a legacy shape; and legacy-compat branches in code ("a pre-vNN save has no X, so assume Y") - the last is the worst, because it silently keeps dead semantics alive in live logic forever. If an old save breaks, it breaks: wipe it and start a new game. KEEP the export-save flow (a live dev convenience, not a compat measure). Revisit only once the game is in front of real players. When a change would previously have earned a migration, say so in the sprint doc and move on.

## Sprint workflow (mandatory)

All development happens in sprints, driven by the roadmap. Sprint docs live in `docs/sprints/sprintXX.md`. For every sprint:

1. **Read** the roadmap and planning docs (and the previous sprint doc, if any).
2. **Design** `sprints/sprintXX.md` from them: goal, definition of done, task breakdown separating Claude-implementable tasks from user-only tasks (purchases, account setup, anything behind the data air gap). **The design MUST open with a reuse analysis (directive 16): an explicit "new mechanisms" vs. "existing mechanisms to reuse" split, mapping every part of the feature onto what already exists before any code is written.**
3. **Implement** all actions in the sprint doc.
4. **Test** new implementations where relevant; run the project checks and show output.
5. **Hygiene:** clean up, update docs (including this file and the sprint doc's checkboxes) so they accurately track project state. Also check `TODO.md` (repo root) - deliberately deferred items that aren't tied to any specific future sprint number, so they don't get lost in sprint-doc archaeology. Add to it when you defer something with no sprint attached; remove from it once something gets actioned.
6. **Commit** (with user approval per the git safety rule).

## What this repository is

**Midnight Garage** - a browser-based, turn-based garage management sim set in 1995 Japan (synthwave pixel art, JDM car culture, hunt-build-sell loop). Solo-dev passion project, ~13-month roadmap to a free itch.io launch.

**Current state:** Sprints 00 through 39 are implemented and committed (`git log` has every hash); the Loop Rework arc (Sprints 25-31) and the Progression Rework arc (Sprints 36-39) are both complete. Sprint 32 (post-arc playtest groundwork) is also committed: the stock-part baseline model (every car-part slot now holds a part or is genuinely empty/missing; a generic brand-neutral stock part is the default and is buyable; aftermarket street/sport/race pushes value above book; a missing slot is a value-tanking defect to fill; Dexie v21) plus a catalog normalization to 4 tiers per component. It ships with a KNOWN, documented balance regression the maintainer deliberately deferred to a later balancing pass: the days-to-`local` hard invariant fails (p50=55, competent-policy's reputation loop stalls, most likely the missing-slot mechanic plus bots not yet handling missing slots, and/or the catalog reprice shifting derived service-job economics), see `TODO.md`'s Open balance/economy questions and `sprint32.md`'s Exit. The maintainer playtest on 2026-07-11 (raw notes in `docs/playtest-notes-2026-07-11.md`) found the core repair/replace loop unsound: too-coarse component granularity, authored numbers where derived ones belong (job payouts blind to part prices, issue fix costs blind to car value, static book value), a dead market (one-shot auction demand rolls, weekly batch dumps, guaranteed-sale listings), and raw ids leaking into UI copy. The triage produced the **Loop Rework arc, Sprints 25-31**. Sprint 25 (triage/guardrails) is implemented: next-morning service-job car arrival (with a real in-transit UI state, not just a silent block), a real fix for a standard-parts-delivery off-by-one, an interim auction-pacing fix (the demand ceiling now re-seeds daily so a lot stuck below reserve isn't permanently dead, and the turnout badge can no longer overclaim "packed"), book value removed from the auction UI, every raw component id in player-facing copy replaced with a real display-name lookup, a drag text-selection bug fixed, a permanent em-dash guard test, equipment-requirement hints moved to tooltips, and four guaranteed-loss service-job payouts repriced with a permanent 1.2x-floor content invariant plus a new reputation gate on install-kind offers. Full detail is in `docs/sprints/sprint25.md`'s Exit section; the `days-to-local` pacing invariant it flagged as failing (p50 dropped to 12, below the [15,35] floor) is back in band (p50=16, hard-gated PASS) after Sprint 26's rework, so that TODO item is retired. **Sprint 26 (the banded 29-part model) is now implemented and verified:** five named condition bands (scrap/poor/worn/fine/mint) as the only per-part condition state, whole-grade repair to a player-chosen target band gated by a 3-tier repair level (yen cost level-independent), terminal scrap (unrepairable, scrap-sell only), a universal forced-induction slot, the hidden-defect/inspection system paused and removed, a group-level addressing bridge (per-part staging deferred to Sprint 28), and a Dexie v15->v16 save migration. Verified: 647 tests green (incl. a 37-test `bands.ts` suite), coverage thresholds pass, full balance run all hard invariants pass, with several aggressive bot strategies underperforming as the expected pre-Sprint-27 seam (they price against bands without the transparent value model yet; Sprint 27 re-bases them). The catalog was also seeded 20->119 parts (Sprint 28 content, prices are maintainer-tuning bait). Sprints 27, 28, and 29 are now implemented and committed: 27 transparent cost-weighted value (clean value minus restoration bill, which also required coupling the auction reserve to the new guide value, pulling Sprint 30's decision 2 forward, after the balance harness showed a value/reserve decoupling had seized the market) plus full pre-bid condition visibility; 28 the drill-down UI + per-part repair/replace addressing (Dexie v17); 29 service-jobs framework v2 (derived payouts with a profitability invariant that structurally retires the guaranteed-loss bug, tier-gated daily offers, Dexie v18, and a fixed competent-policy bot bug that had been zeroing its reputation, which prompted a maintainer-approved days-to-`local` band retune from [15,35] to [10,35] since the probe itself changed). Sprint 30 (living auctions) is now implemented and committed: age/mileage curves in clean value, a daily bidder-interest process (turnout-as-bidder-count) replacing the one-shot demand ceiling, staggered daily lot arrivals replacing the weekly dump, and Sprint 25's interim auction patches removed (Dexie v19 for the added lot turnout field). Its mechanics pass all hard balance invariants, but a first-pass tuning issue is flagged for the maintainer's playtest (the sprint's own user-only task): auctions currently read as a fire sale (94% of wins are cheap steals) and flipping is a net loss, driven by arrival flooding + too-weak bidder contestation + age/mileage value depression, all content-tunable, see `sprint30.md` Exit and `TODO.md`. Sprint 31 (walk-in-only selling) is implemented and committed, closing the Loop Rework arc: public listings removed entirely, replaced by a daily walk-in offer stream (`carsForSale`/`pendingOffers` state, Dexie v20). Also on 2026-07-11: the em-dash ban (directive 15) landed with a repo-wide purge and a permanent guard test.

Three more sprints landed before the Progression Rework arc began. **Sprint 33** (equipment tiering content pass) surfaced and fixed two real pre-existing bugs rather than papering over them: bots had no way to remove a part before replacing an occupied slot (a new `removeParts` DayAction wired every bot up to the player's own existing Remove-then-Replace two-step), and `serviceGrinderStrategy`'s repair-only identity had reintroduced the exact Sprint 16 catch-22. **Sprint 34** unified car generation into a single chain (year -> age -> mileage range -> roll mileage -> condition range -> roll condition -> per-part jitter) and fixed a real condition double-count (`installedPartsValueYen` no longer separately discounts by band; a part's condition is now priced exactly once, through the restoration bill); golden hashes re-pinned. **Sprint 35** added customer-owned parts: a part pulled off a service-job customer's car is no longer discarded but kept in the player's own `partInventory`, tagged `customerJobId`, locked from sale/scrap, reconditionable in place through the SAME repair economy as an on-car repair (a new `recondition-part` job kind working a loose inventory part on the bench), and reconciled out at the job's close-out; Dexie v22.

**The Progression Rework arc, Sprints 36-39, is now implemented and committed**, replacing the old binary equipment-ownership gate with the model `docs/design/progression-bible.md` now canonically defines. **Sprint 36** introduced six always-owned tiered tool lines (engine/drivetrain/suspension/wheels/body/interior, tiers 1-3) in place of one-shot equipment purchases - tier 1 is owned from day one on every line, and a higher tier speeds labor rather than unlocking access (Dexie v23, migrating legacy equipment ownership onto a frozen tier map). **Sprint 37** built the job ladder content on top of it: a full 32-template service-job rewrite with an explicit per-task `minToolTier`, plus the one real own-car capability ceiling (`toolCeilings.naToTurboConversionEngineTier`, gating a factory-NA-to-turbo conversion behind engine tier 3). **Sprint 38** added Specialty, reputation's horizontal complement: a six-way `specialty` record earned from service-job work (split across every distinct component group a job's tasks touch), weighting which job templates get offered and paying an in-lane premium on payout once a discipline clears a threshold; Dexie v24 (additive). **Sprint 39** closed the arc with Techniques (named craft unlocks per discipline, threshold 120 specialty points, each gating one new tier-4 signature service-job template) and a derived Shop Title surfaced diegetically in the garage-screen header - both pure functions of `state.specialty`, zero new persisted state, no save-version bump needed. Full arc detail and the balance-harness numbers at each step are in `docs/sprints/sprint36.md` through `sprint39.md`'s Exit sections.

On 2026-07-12 the GDD (bumped to v0.6) and the roadmap were amended to match this shipped state (labor slots, commissions, the tool-gate model, Specialty); the same day, three real engineering bugs flagged in `TODO.md` were fixed: `investor.ts`'s bot never actually completed an install (a same-tick buy+install race that always failed `installFitGate`, now split across two ticks) and picked parts by group instead of the specific empty slot (now slot-precise); and a customer-owned tagged part could be installed onto a different car than the one it was pulled from, escaping service-job close-out reconciliation (now gated both sim-side, `installFitGate`, and in the Replace-drawer candidate list).

A maintainer playtest on 2026-07-13 (raw notes in `docs/playtest-notes-2026-07-13.md`) triaged into Sprints 40-43. **Sprint 40** (honest job offers, band choice, a main menu) is committed: offer generation now validates every task against the rolled customer car at generation time (killing the "work done before the car even arrived" display bug at its root), the player gets full granular repair-band control at both group and per-part granularity via a shared `BandPicker` (replacing the old hardcoded mint-only/fine-only limits), and a temporary main menu (New Game confirmation) closes the one-click career-loss footgun. **Sprint 41** (tier-scaled repair costs and true consumables) is committed: restoration cost now scales by the car's own roster tier (`restoration.partsCostFactorByTier`, shitbox repairs ~0.12x of rare-tier ones), tyres/brakePadsDiscs/clutch became replace-only (never repairable), and the value formula's `hassleFactor`/`floorFraction` were retuned alongside it; no save-schema change. Full detail in `docs/sprints/sprint40.md`/`sprint41.md`'s Exit sections. **Sprint 42 (the flip ledger) is now implemented and committed:** every owned car gets a per-car money ledger (purchase price at auction win/buyout, running repair spend, running installed-part spend) plus a `pricePaidYen` on each `PartInstance`, surfaced on CarDetailScreen as a Finances panel (purchase, repairs, parts, total spent, guide value, restoration bill remaining, and a live projected profit colored by sign) above the sell section - no sale-price control anywhere (maintainer decision 2, playtest item 7). Pure bookkeeping bolted onto existing resolvers (auction win/buyout, repair-job creation, part purchase/delivery, install completion, sale): proven zero economic impact via a day-by-day cash-trace diff against a pre-sprint worktree checkout (byte-identical) and a full balance-harness run whose every disclosed figure matches Sprint 41's exactly; Dexie v24->v25 (additive). Full detail in `docs/sprints/sprint42.md`'s Exit section.

On 2026-07-13 a real playtest bug was found and fixed: `generateDailyAuctionArrivals` was called with `next.day` (still 1 on the very first `advanceDay` tick) instead of `next.day + 1`, colliding with `createInitialGameState`'s own day-1 seed batch under the shared `lot-${day}-${tier}-${i}` id scheme - two distinct lots shared one id, so winning one could immediately log as lost. Fixed with the one-line day-offset correction (mirroring the identical Sprint 29 service-job-offer hazard), two permanent regression tests added, golden hashes re-pinned after confirming behavior-neutrality; the balance harness shows real auction win counts improving (83,448 -> 88,134 across 1000 seeds), confirming it as a genuine fix, not just a hash churn.

**Sprint 45 (the double-parking grace slot) is now implemented**, closing the maintainer's same-day follow-up on the design flaw the bug above exposed: an acquisition (auction win, instant buyout, or service-job accept) could still fail outright the instant parking was full, with zero player agency between "still winning" and "car evaporates." Acquisition capacity now checks parking OR any open service bay (`hasAcquisitionSpace`/`assignToShop`, `facilities.ts`), not parking alone; beyond real capacity, one always-present unowned "grace" overflow slot (`GameState.graceParkingCarId`, Dexie v25->v26) absorbs a car that would otherwise be lost, rendered red ("double parked") on the garage screen and costing a daily `DOUBLE_PARKING_FINE_YEN` (8,000, tuning bait) every End Day it's still occupied - migrating into real capacity automatically (fine-free) the moment a slot opens up, via a new day-boundary step in `advanceDay.ts`. Only once parking, every service bay, AND the grace slot are all full does an acquisition genuinely fail (`acquisition-blocked`'s `no-parking` reason renamed `no-space` for accuracy). The auction screen's capacity warning now distinguishes "will double-park (a fine)" from "will genuinely be lost." Full gate green (922 tests), all balance-harness hard invariants pass; see `docs/sprints/sprint45.md`'s Exit for the full verification writeup, including an honest disclosure that this harness run reflects the full uncommitted tree (Sprint 44's cost rebase and the auction fix above are also live in it, not isolated to Sprint 45).

A second maintainer playtest pass on 2026-07-13 triaged into the **Legibility & Trust arc, Sprints 46-52**, now complete. **Sprint 46** (auction truth) is implemented: a real off-by-one in `auctionCloseLabel`'s "final call" backstop fixed, every auction `DayLogEntry` now stamps `modelId`/`year` so day-log lines name the actual car instead of a raw lot id, and "dealer leads at X" reworded to "leading bid X". **Sprint 47** (the profitable flip) rewrote the core repair/value economy to kill a real guaranteed-loss bug: `chargeRepairWork`'s hidden per-job consumables fee was deleted outright (Sprint 47's plan cost now equals the charged cost, by construction), and `marketValueYen` moved from a hard mint-referenced floor-clamp (which created a "dead zone" of zero marginal repair return) to a two-slope premium model referenced against a new fine-targeted `carValuationBillYen`, plus a per-car upkeep-tier roll (neglected/average/cherished) layered onto auction-car generation. Proved with two deterministic acceptance probes (a "sane flip" at a +253,035 yen margin, a "salvage flip" at +136,800 yen), full balance harness run clean (days-to-`local` p50=13). **Sprint 48** (the car page: plan, price, decide) reworked `CarDetailScreen.vue`'s repair interaction per a maintainer correction mid-arc (a slider-style band picker was rejected as clunky in favor of one click per condition rung): `BandPicker.vue` is deleted; every repair control (group, per-part, and the inventory bench recondition) is now a single button that climbs exactly one band per click, priced/labored off the real marginal repair-plan diff (`game.nextRepairStep`/`nextReconditionStep`); a new sim helper `previewPlannedWork` powers a pre-Confirm Finances estimate block (planned cost, bill/value/profit "after"); a single global condition filter replaces the old per-group good-order toggles; and every player-visible "staged" -> "planned" copy rename landed with a permanent guard test. A real bug (the group/per-part "Clear planned repair" control vanishing once a plan reached the mint ceiling, since it shared its `v-if` with the now-null "advance further" button) was found and fixed during implementation. Full gate green (938 tests); balance harness skipped as a pure UI/interaction rework with no sim-economics change. **Sprint 49** (parts market) rebuilt `PartsMarketScreen.vue` into a hero-grid home view over department/browse-everything catalog views (`enterDepartment`/`enterBrowseEverything`/`returnHome`), with a sticky cart rail. **Sprint 50** (auction cards) replaced the auction house's old expandable condition report with real-world-style Japanese used-car auction grades: a new `computeAuctionGrade` (`packages/sim/src/auctionGrade.ts`) partitions the six component groups so nothing is scored twice - mechanical groups (engine/drivetrain/suspension) drive the overall number grade only, body+wheels drive the exterior letter, interior drives the interior letter - surfaced on the auction card as a single "Grade X - Ext Y - Int Z" line plus a promoted restoration-bill figure and a real bids table; the underlying condition/band system itself was untouched, per an explicit maintainer correction mid-sprint that this was a display-only change. **Sprint 51** (chrome) added a global Escape-key handler with a strict priority order (input focus, then CarDetail's own pick session, then any open modal, then the End Day confirm state, else navigate to `/menu`), consolidated six per-screen End Day buttons into one app-wide floating button, added a Save section to the new main menu, gated the dev-only `/spike` route behind `import.meta.env.DEV` (confirmed via a production build that the whole PixiJS chain drops out of shipped chunks), and added a permanent `copyGuard.test.ts` banning dev-facing words ("gate", "staged", "dev") from any screen's player-visible HelpHint copy. **Sprint 52** (pacing) closed the arc: a content-tunable `serviceJobs.offerCountCapByDay` step curve ramps a new player from at most one offer on day 1 to the full distribution by day 12 (`offerCountCapForDay`, a clamp on the existing daily draw, no change to expiry/templates/payouts); tool tiers 2 and 3 now additionally require a live "classifieds" listing for that exact line+tier (`GameState.machineListing`/`nextMachineListingDay`, Dexie v26->v27) - reputation still sets eligibility, but only one used machine is ever purchasable at a time, posted every 4-8 days for a ~3-day window (`rollMachineListings`, consumed on purchase so a bought listing never lingers stale; bots needed zero code changes since `considerToolUpgrade` already retries daily); and the Upgrades screen was rebuilt with facilities as cards in the same visual language as the tool wall, a new Classifieds section, and every "gate"/"rung" HelpHint reference rewritten into diegetic shop-owner copy (its Sprint-51 copy-guard exemption was removed once the rewrite landed). Full gate green (979 tests), balance harness confirms the hard-gated days-to-`local` pacing invariant held exactly at Sprint 47's p50=13 baseline despite both new throttles. See `docs/sprints/sprint46.md` through `sprint52.md`'s Exit sections for full per-sprint detail.

**Sprint 43 (the tool wall) is now implemented**, out of order, after being designed on 2026-07-13 and then skipped over when work moved straight to Sprint 44 without it - caught and flagged by the maintainer (git log showed the last real commit was Sprint 42, with 43's own Exit still unfilled), who approved implementing it now before committing anything. Tool tiers 2 and 3 (never tier 1) now gate on reputation exactly like bays do (`minReputationTier` on `ToolLineTierSchema`, `local` for tier 2 and `known` for tier 3, uniform across all six lines), amending the progression bible's former "tools have no reputation gates" law with the original rationale kept as history. `nextToolTierRepGate`/`applyToolUpgrade` (toolLines.ts) mirror the bay-gate shape exactly; the Upgrades screen's Tools section became a 6-column x 3-rung tool wall with a click-to-open info box (unlocked job templates, the engine-tier-3 NA-to-turbo ceiling, the tier's labor-speed effect) derived live from the real catalog. Fixing this sprint's own downstream test fallout surfaced a real, substantial, maintainer-confirmed finding: Handyman (0/30 seeded careers) and Cautious Restorer (2/200) now essentially never upgrade any tool line at all, since neither bot has a realistic route to reputation (no service jobs, sales rarely clear the clean/concours bar) and even tier 2 now requires it - confirmed as the simulation correctly exposing a bot playing the game in an unintended way, not a gate miscalibration, and deferred to `TODO.md` for a future bot-behavior pass rather than fixed here. Full gate green (931 tests), all balance-harness hard invariants pass; see `docs/sprints/sprint43.md`'s Exit for the full verification writeup and the disclosed cash-curve shift (most non-passive bots' day-100 cash rose, since cash that used to fund now-refused tool upgrades stays unspent and feeds into stronger auction bidding instead).

A third maintainer playtest on 2026-07-14 (raw notes in `docs/playtest-notes-2026-07-14.md`) found the core repair loop still paying a net loss on the exact play it should reward (buy a cheap wreck, repair it, the guide value barely moved) - triaged into the **Economy Rebuild arc, Sprints 53-55**, and three more UI/polish sprints (56-58). **Sprint 53 (fitment-class parts, economy-bible.md law 3) is now implemented and committed**: a new canonical `docs/design/economy-bible.md` documents the arc's four laws and the full playtest diagnosis (a flat tier-blind parts catalog, a Sprint-47 value-floor regression, a guaranteed-loss discount zone, and no generation-time value-trap guard). This sprint fixes Law 3 only (repair costs proportionate to the car): every component slot now ships 16 real, separately-named catalog SKUs (4 fitment classes - shitbox/common/uncommon/rare, diegetically "Kei & Compact/Family/Sports/Grand Touring" - x 4 grades), never a runtime price switch, with every SKU's price resolved at content-load time from a centralised `partPricing.json` sheet (`baseCostYen x classFactor x gradeFactor x globalFactor`, plus a sparse per-SKU override map) so a whole-market rebalance is a handful of multiplications, not a mass content edit. The pre-Sprint-53 116-part catalog is kept unchanged as the `common` class (preserving every historical save-migration test); 348 new class-prefixed SKUs were generated from it. `partFitsCar` (the one fit choke point already used by the install gate, the Replace drawer, and every bot's part-purchase filter) gained a class-match clause, so cross-class arbitrage is now physically impossible - resolving Sprint 44's donor-car-arbitrage rationale through fitment instead of a flat catalog. Dexie v27->v28: a real (non-additive) migration remaps every owned/lot/service-job car's installed parts from their implicit pre-Sprint-53 `common` class to their own model's real fitment class. Full gate green (986 tests), balance harness clean (days-to-`local` p50=12, against the [10,35] hard-gated band and Sprint 52's p50=13 baseline). Deliberately deferred to Sprint 54: the value formula itself (the two-slope model and its 0.05 floor) is untouched this sprint, so the value trap the playtest hit is not yet fixed - only its "brake pads cost twice the car" structural cause is. See `docs/sprints/sprint53.md`'s Exit for full detail.

**Sprint 54 (the value law, economy-bible.md laws 1-2) is now implemented and committed**, closing the value-trap bug the playtest actually hit. `marketValueYen` moves from Sprint 47's two-slope premium (a 60%-loss zone above a threshold, plus a floor that had silently regressed 4.4x and become load-bearing) to ONE slope, always above 1 (`valuation.marketRepairDiscount`, 1.2 first-pass, schema-enforced `.min(1)` as a structural law) - every repair yen returns more than itself, at every reachable state, by construction; at zero remaining bill the formula returns exactly clean value, so a fully restored car can never be worth more than the identical clean car (the no-inflation ceiling is structural, not a clamp). The separate fine-referenced valuation bill retires outright: the market now prices against the exact same mint-referenced bill the player already sees as "restoration bill remaining," closing a player-visible inconsistency for free. `generateAuctionCarInstance` (shared by both the auction catalog and service-job customer-car rolling) gained a generation-time guard: any rolled car whose bill would exceed `partsGeneration.maxBillFraction` (0.7) of its clean value is deterministically softened - band damage lifted first, missing slots filled only as a rare fallback - so every generatable lot is mathematically guaranteed a positive full-restore margin (proven in the sprint's own design work: at the worst permitted roll, margin >= 0.22x clean value). Four new acceptance-probe families in `valueModelProbes.test.ts` (the Honda City probe re-running the exact playtest scenario, a full-restore-per-tier probe, a no-free-lunch probe, and a ceiling probe) plus a fifth proving the scrap-value floor never actually binds on a generated lot. Full gate green (996 tests), balance harness clean (days-to-`local` p50=12, unchanged from Sprint 53) with the anticipated cash-curve shift confirmed in the right direction: Handyman's day-100 median cash moved from below Passive Grinder to above it, since repairing now genuinely pays. Law 4 (the full machine-checked economy-coherence audit) remains Sprint 55 scope. See `docs/sprints/sprint54.md`'s Exit for full detail.

**Sprint 55 (the global coherence system, economy-bible.md law 4) is now implemented and committed**, closing the Economy Rebuild arc. `packages/sim/src/coherence.ts`'s `computeRosterCoherence` derives four closed-form facts per roster model by calling the real Law 1/Law 2 sim functions directly against a deliberately worse-than-generation-could-ever-roll car (every real slot at scrap, at the roster's worst reachable mileage, then run through the actual `enforceMaxBillFraction` guard, now exported): the bill-to-clean ratio (Law 2), the flip margin buying at reserve + fully restoring + selling at guide would clear (Law 1), and the full tyres+brakePadsDiscs+clutch consumable-replacement share of book value (Law 3, gated against a new `coherence.maxConsumablesShareOfBookValue` content anchor, 0.15). All render as a per-model table in `tools/balance/report.md` and hard-gate `python -m balance.cli check` alongside a fifth check confirming the service-job payout margin floor still clears its Sprint 29 profitability invariant - a maintainer can now eyeball the whole roster's economy on one page, and a coherence regression fails the build rather than waiting for a playtest to find it. `economy-bible.md`'s Anchor Inventory is promoted into a full audit table, machine-checked against `economy.json`'s real top-level key set (a new content-integrity test fails if the two ever drift apart). The sprint's own retune pass, applied only after re-measuring showed a real problem: the historical 84%-steal auction "fire sale" TODO item had flipped into the opposite extreme once Sprint 54's gentler value law raised anchorValueYen (36.1% frenzy against a 15% ceiling, steal down to 7.3%) - `AUCTION_WHOLESALE_FRACTION` moved 0.85 -> 0.75 to pull rival private valuations back down, and `selling.offerSpread` moved `[0.82, 1.12]` -> `[0.90, 1.08]` (raising the walk-in-sale floor so a bad roll can no longer erase the worst-case flip margin, while keeping the spread's own mean at 0.99 so the Sprint 54 no-free-lunch invariant still holds). Book values and the mileage-curve floor stayed untouched - the coherence table showed no model out of line at either. Full gate green (1001 tests, coverage 91.23%/81.44%/92.17%/95.13%), balance harness clean and dramatically improved: days-to-`local` p50=13 (still hard-gated in-band), the auction win-price tails now sit inside their target band for the first time this harness has ever recorded (steal 11.7%, frenzy 12.6%), and both previously-chronic informational invariants now pass for real (most active strategies beat Passive Grinder's day-100 cash; Flipper clears its own starting cash, Y1,579,076 vs Y1,500,000). The fire-sale TODO item is retired outright. See `docs/sprints/sprint55.md`'s Exit for the full before/after numbers.

A maintainer follow-up the same day (2026-07-14) raised a distinct, unimplemented economy concern - a build with expensive aftermarket mods but neglected foundational parts (worn brakes, rusted body) can profit under the current additive value formula the same as a coherent build would, which doesn't match real-world buyer behavior. Captured in `TODO.md` under "Open balance/economy questions" as a large, unscoped economy adjustment (no solution decided), flagged to revisit after Sprint 58.

**Sprint 56 (the auction card: grade stamps and the two-panel redesign) is now implemented and committed** - pure presentation, no sim or economics change. A new `GradeStamp.vue` component renders each of the three `computeAuctionGrade` grades (overall/exterior/interior) as a chunky ink-stamp box, color-ramped green/sodium-amber/red with `R` (the structural-defect flag) getting its own distinct deepest-red tone, muted by default and reaching full saturation only on the hovered/focused card (rule-of-glow compliance, reached into via `:deep()` from the parent). `AuctionScreen.vue`'s lot card is restructured into a two-panel grid per the maintainer's mockup: a 320px left panel (title, turnout badge, the art placeholder doubled to 320x160, the three grade stamps) and a flexible right panel (guide value, reserve, leading bid, restoration bill, close label, then the bid stack - a raise-to field flanked by `-`/`+` stepper buttons deriving their step size from the lot's own real bid-ladder increment, a large Place/Raise button, Buy Now beneath it). The old per-group `BandChip` condition row is deleted from the auction card entirely, amending Sprint 26 decision 10/Sprint 27's pre-bid-transparency law by explicit maintainer decision - full per-part truth still lives on the car detail screen after acquisition, the grade trio is the new pre-bid read. `auctionCloseLabel` drops the "(any bid resets the clock)" parenthetical. Full gate green (1018 tests, coverage 91.17%/81.41%/91.71%/95.02%); no balance harness run (display-only, matching Sprint 50's own precedent). Not done: I did not start the dev server to eyeball the result in a browser (starting long-running processes is outside what I run myself) - that, plus confirming or redirecting the steppers-vs-rotary-dial art-bible exception, is the sprint's own listed user-only task. See `docs/sprints/sprint56.md`'s Exit for full detail. A same-day follow-up polish pass against the maintainer's own running dev server fixed real issues the design-only review missed: blurry rotated stamp text (smaller angle, larger stamps, later made genuinely per-stamp random rather than a fixed repeating pattern), an undersized bid input/steppers, a `<label>` wrapping all three bid controls at once (fixed to associate with just the input), a wrapping title/spec line, and a top-bunched right panel (now a centered current-price headline, secondary guide/reserve/bill, a real day-count timer chip, and a two-row bid/action block).

**Sprint 57 (jobs close where they open: completion on the jobs screen, and an honest post-job report) is now implemented and committed.** A new per-job spend ledger (`GameState.serviceJobLedgers`, `packages/sim/src/serviceJobLedger.ts` mirroring the Sprint 42 car-ledger shape) is written at the two places a customer-job repair or install was ever actually charged - previously silent no-ops for a customer's car - and read once at close-out before being deleted. `service-job-completed`/`-failed` log entries replace the old install-only, catalog-price-reconstructed `partCostYen`/`profitYen` with four honest fields present on both outcomes: `repairCostYen`, `partsCostYen` (real spend off the ledger, never a catalog guess), `specialtyGained` (per-group, `applySpecialtyDelta` now returns its own deltas), and `netProfitYen` (payout minus real spend, negative-only on a failure - sunk cost, shown without flinching). `JobCompleteModal.vue` renders all of it. Completion itself moves to `ServiceJobsScreen.vue`'s "In the shop" rows (the exact same button `CarDetailScreen.vue`'s service banner used to own); the car page keeps the status text and points to the job board for the actual action. `SAVE_VERSION` 28 -> 29 (purely additive). Full gate green (1029 tests, coverage 91.36%/82.22%/92.18%/95.24%); no balance harness run (bookkeeping plus a UI move, zero economic change, the same argument Sprint 42 proved for its own identical shape). See `docs/sprints/sprint57.md`'s Exit for full detail.

**Sprint 58 (chrome cleanup: one menu title, one save surface, and a parts market that shows its cart) is now implemented and committed**, closing the Economy Rebuild arc's trailing UI/polish sprints (56-58). Pure UI relocation, deletion, and content-copy work, no sim surface touched. `MenuScreen.vue`'s duplicate `<h1>` is deleted (the app chrome's title is the only one on `/menu` now) and its inline `.load-panel` is deleted too, leaving `SaveMenu` as the single save/load/export surface - the three load-behavior tests that exercised the deleted panel ported onto `SaveMenu`'s own `import-save` flow (previously untested in isolation), closing a real coverage gap. `PartsMarketScreen.vue`'s cart rail now renders inside a `.market-layout` shell that wraps every view including home, not just the department/browse-everything branches, so cart contents are visible from the very first screen; a new `< Back` button sits beside the breadcrumb in both catalog views. The `suspension` component group's display name changed to "Suspension and Brakes" (one string in `componentDisplayNames.json`, read through the existing `componentLabel()` choke point everywhere it renders), and the two dead pre-Sprint-26 keys (`brakes`, `forcedInduction`) were dropped from `ComponentDisplayNamesSchema` and its JSON, with the content test now asserting the key set is exactly the 6 real groups. Full gate green (1033 tests, coverage 91.45%/82.31%/92.36%/95.33%); no balance harness run (zero economic surface touched). See `docs/sprints/sprint58.md`'s Exit for full detail.

A fourth maintainer playtest pass on 2026-07-14 (the second that day, against the committed Sprint 58 build; raw notes in `docs/playtest_notes/playtest-notes-2026-07-14-pass2.md`) produced 19 items, triaged and designed as **Sprints 59-65**: 59 the earned-yen economy retune; 60 the foundation law (economy-bible Law 5: a hierarchy of needs gating the aftermarket premium by the worst foundational part, graduating the TODO build-coherence capture into a designed system); 61 honest job offers via baseline-tracked install tasks (a maintainer mid-triage direction: track the original part instead of Sprint 40's slot-clearing hack, which was manufacturing missing-part cars whose flavour text contradicted them), plus class chips and customer cars in the parts-market fit filter; 62 the standing view (granular reputation/specialty, a recorded progression-bible Law 4 amendment); 63 the work-order UI cleanup, an honest Confirm (planned labour, not remaining), the "Wheels and Tyres" rename, and the British-spelling sweep enforcing new directive 18; 64 the morning-report redesign (wins read as wins, noise aggregated, "1 lots" fixed) and a demoted, two-step Buy Now; 65 chrome (a real full-screen main menu via route-meta chrome gating, one save surface, the gate-text-to-tooltips sweep, symmetry min-heights, honest part affordances). Remaining implementation (60-65) is gated on per-sprint maintainer approval per the standing cadence; 59 before 60 (the flip probes must survive the foundation law), the rest are order-independent.

**Sprint 59 (the earned yen: instant flips, job margins, and starting cash) is now implemented and committed.** The ¥156k unimproved-flip bug traced to rivals bidding at a wholesale 0.75 ceiling against a 0.5 reserve - fixed by contestation, not a reserve floor: `AUCTION_WHOLESALE_FRACTION` 0.75 -> 0.97 (rivals now price near guide value; a maintainer-rejected first instinct, a ~0.9 reserve floor, would have flattened bidding into a dead sliver instead), `AUCTION_RESERVE_PRICE_FRACTION` 0.5 -> 0.6 (stays a pure seller floor), `selling.offerSpread` narrowed to `[0.93, 1.05]`. A new `unimproved-flip probe` (`valueModelProbes.test.ts`) proves the median flip nets within 7% of the purchase price per roster tier (measured 2.5-5.7%, always on the profit side, down from ~49%). `serviceJobs.marginMin`/`marginMax` cut `[1.4, 1.65]` -> `[1.18, 1.35]` (the Law 4 floor of 1.15 still clears with headroom) - the tyre exemplar drops from a ~¥47k windfall to ~¥20k paid work. `STARTING_CASH_YEN` cut 1,500,000 -> 300,000, derived (not asserted) from real roster medians: a ¥271,077 floor (buy at reserve + full restore + 4 weeks' rent + a parts float), with a real margin kept above it. Full gate green (1037 tests, coverage 91.26%/82.04%/92.36%/95.22%); balance harness run, all 9 hard gates pass (days-to-`local` p50=12, unchanged). Real, disclosed consequences of the fix rather than regressions: a disciplined bidder's win rate at guide value dropped 98% -> 41% (this IS the fix), the auction win-price tail inverted from steal-dominant to frenzy-dominant (72.2% frenzy - the tail bucket's own 90%-of-guide threshold is now stale and flagged for a future recalibration, not patched here), and competent-policy - purpose-built to climb the reputation ladder - now affords zero tool-tier upgrades within its 100-day harness window (folded into TODO.md's existing Handyman/Cautious-Restorer tool-lockout finding for a future combined bot-tuning pass). See `docs/sprints/sprint59.md`'s Exit for the full before/after numbers.

**Sprint 60 (the foundation law: no value for chrome on a deathtrap, economy-bible.md Law 5) is now implemented and committed**, graduating the "build coherence" TODO capture into a designed system. `marketValueYen` becomes `baseValue + foundationFactor(car) x installedPartsValueYen` - the ONLY change is that the additive aftermarket premium is now scaled by the worst foundational part's factor (`marketValue.ts`'s new pure `foundationFactor`, over the car's own bands/missing state). New content anchor `valuation.foundation` (`parts`: tyres, brakePadsDiscs, brakeCalipersLines, steering, chassis, underbody; `factorByState`: missing 0.10, scrap 0.15, poor 0.45, worn-or-better 1.0), schema-enforced monotonic and capped at 1 (withholds premium, never inflates). The base term is untouched, so Law 1 holds unchanged and repairing a failed foundation returns its repair value PLUS the released premium (foundations first, then the toys count). The maintainer's verbatim kei-truck build (race engine/turbo/cosmetics on scrap brakes and a rusted body) is now a permanent losing-money probe (`valueModelProbes.test.ts`); the marginal-return and "sound foundations recover the premium" behaviour are proved in `marketValue.test.ts`; the coherence table is arithmetically unchanged (its all-scrap-STOCK probe car carries zero premium, so the factor is inert - asserted directly). `CarDetailScreen.vue`'s Finances panel names the failing foundational parts and the withheld yen when a bad foundation is holding value back. Law 5 recorded in `economy-bible.md` with the maintainer's worked example (the sprint approval is the required amendment approval). Full gate green (1051 tests, coverage 91.31%/82.10%/92.40%/95.25%); balance harness re-run is byte-for-byte identical to Sprint 59's (bots never install aftermarket parts, so the factor is inert across every career), all 9 hard gates pass, days-to-`local` p50=12 unchanged, no golden hash moved. First-pass factor numbers are maintainer-tuning bait. See `docs/sprints/sprint60.md`'s Exit for full detail.

**Sprint 61 (honest job offers: baseline-tracked installs, class chips, and the fit filter) is now implemented and committed.** The root fix for the "customer says the tyres are worn but the car has no tyres" bug (the maintainer's mid-triage direction: "just keep track of the original part"): `ServiceJob` gains `baselineInstalledPartIds` (the `PartInstance.id` in each install-task slot at generation), and an install task is done only once a DIFFERENT qualifying part is fitted. `forceTasksOutstanding` stops clearing install slots (Sprint 40's hack that manufactured missing parts) - the customer's original part stays present, rolled down to a neglected band so the complaint is honest, and its id is the baseline. Threaded through the bot helper and the store's per-task `done` view; `SAVE_VERSION` 29 -> 30 (additive, two golden-save tests). A flavour review confirmed all 25 install templates already read as upgrade/wear, never missing-part, so no content edit was needed. Class chips (`fitmentClass` on both job views and `LotDetail`) render "Kei & Compact"/"Sports"/... on auction lots and job cards; a new `partsFitVehicleOptions` store getter puts owned AND inbound customer cars in the parts-market "fits this vehicle" filter, so the accept-job/order-parts/both-arrive-tomorrow loop works. Full gate green (1059 tests, coverage 91.50%/82.05%/92.91%/95.33%). Balance harness re-run (a real sim change): all 9 hard gates pass; days-to-`local` p50 moved 12 -> 16 (still in the [10,35] band, no band-move needed) because an install job now takes one extra tick (remove -> buy -> install onto the present original part instead of buy -> install onto an empty slot), slightly slowing the service-job reputation loop - service-grinder/competent-policy day-100 cash dropped accordingly (441k/435k from 572k/643k), the honest, disclosed cost of the more-correct install semantics; no golden career hash moved. See `docs/sprints/sprint61.md`'s Exit for full detail.

**Sprint 62 (the standing view: reputation and specialty on the shop wall) is now implemented and committed.** A new `StandingScreen.vue` at route `/standing` - the one place the shop's granular standing lives: exact reputation points with the named next tier, all six specialty disciplines with their points and named tier-4 technique (shown whether earned or not), and the derived shop title, all in words and tables, no meters. It's a pure renderer over one new store computed `standingView` (a pure function of existing state - no new persisted field, no save bump, exactly Sprint 39's derive-don't-store shape). Reached diegetically: the garage-header reputation line and the jobs-screen rep figure both became links to it, so the numbers live in exactly one pull-on-demand place. Progression bible Law 4 was amended (recorded in the doc; this sprint's approval is the required maintainer approval) to permit exact points on this single dedicated screen while keeping the ban on ambient meters/bars/toasts everywhere else; banned vocabulary ("xp"/"mastery"/"level") stays out, guard-asserted in the screen's test. Full gate green (1065 tests, coverage 91.55%/82.14%/92.98%/95.36%); no balance harness run (pure UI over existing derived state). See `docs/sprints/sprint62.md`'s Exit for full detail.

**Sprint 63 (the work order: clean repair rows, an honest Confirm, and British spelling) is now implemented and committed.** Every repair control on the car page is now compact: a `current -> planned` `BandChip` preview, a single **+** climb-one-band button (the "Repair to X" phrase moved to its tooltip) with the incremental cost as a quiet caption, and an **x** to clear - no sentence in any button (`PartCard`'s bench-recondition control got the same treatment). The Confirm button now shows the PLANNED cost and labour (a new `PlannedEstimateView.plannedLaborSlots`, summed with `confirmStagedWork`'s exact accounting) and grows as work is added; the "N labour left today" figure moved to a caption that warns (never blocks) when the plan overruns today. All the plan/stage plumbing and `data-test` hooks are untouched - presentation only. British spelling enforced: the 4 player-visible "labor" sites fixed, plus a new `spellingGuard.test.ts` scanning visible `.vue` template text (script/style/tags/mustache/comments stripped, so code identifiers and CSS never trip it) and `dayLogFormat.ts`'s copy strings for `labor`/`tire`/`color`/`gray` (the `'labor-overbooked'` DayLogEntry type discriminant is correctly exempt as a code identifier). The `wheels` group label became "Wheels and Tyres" (one content string; the `dayLogFormat` test expectation updated per directive 17). Full gate green (1071 tests, coverage 91.58%/82.19%/93.00%/95.37%); no balance harness run (labels and a display-only labour sum, zero sim-economics change, every golden hash held). See `docs/sprints/sprint63.md`'s Exit for full detail.

**Sprint 64 (the morning report: wins read as wins, and Buy Now steps back) is now implemented and committed.** A new game-layer `classifyDayReport` (`dayLogFormat.ts`) turns a day's `DayLogEntry[]` into a structured view (the sim is untouched): win/buyout cards, a `{ earned, onCars, bills }` money split, ordered notable lines (outbid alerts first), and aggregated noise. `DayReport.vue` renders win cards FIRST in green (never red), with the net delta demoted to a small secondary line - so after winning an auction the player sees "WON 1987 Honda City for ¥156,030", not a scary red -¥156,030 (item 13). A shared `pluralise` helper kills the "1 lots" bug at the formatter and in the aggregated noise. Buy Now left the bid-button block for its own separated ghost-styled row and now takes two clicks (arm -> confirm, the End Day precedent), with the disabled (short-on-cash) state explaining itself in a tooltip - it can no longer fire on a stray click against Raise (item 3). Full gate green (1078 tests, coverage 91.34%/82.17%/92.86%/95.04%); no balance harness run (pure presentation over the same `game.buyout`, zero sim change, every golden hash held). See `docs/sprints/sprint64.md`'s Exit for full detail.

**Sprint 65 (chrome: a real main menu, one honest save surface, tooltips not clutter, symmetry) is now implemented and committed, closing the playtest pass-2 arc (Sprints 59-65).** The menu route gained `meta: { chrome: false }`; `App.vue` hides the whole header/nav on chrome-less routes via one `v-if`, the Menu nav tab became a small header menu CONTROL (a mouse player's way in, mirroring Escape), and the menu renders full screen with its own title. Pause-menu semantics: the ui store tracks `lastGameplayRoute` and both Continue and Escape-from-menu return there, so Escape is a true toggle (open from the auction house, Escape again lands back there), garage as the boot fallback. `SaveMenu` dropped its toggle-and-popover for inline full-width buttons (Copy / Load-on-demand / Export), the redundant "Save" heading gone - one honest save surface. A new shared `HintTooltip.vue` (a "locked" trigger with a `role="tooltip"` bubble revealed on hover/focus, keyboard-reachable, never native `title`) replaced every always-visible gate sentence on the Upgrades screen (the facility and tool-wall `.rep-hint`s, the `.listing-hint`, the `.tier-rep-req`); the gated card/node dims with its price still legible and the reason in the tooltip. Explicit min-heights (the ShopSlot precedent) lock the parts-market and upgrades grids so a wrapping label ("Suspension and Brakes"/"Wheels and Tyres") can't re-stagger a row. `PartsMarketScreen`'s `.no-fit` opacity dimming is gone - a fully-buyable part no longer reads as disabled; fit status lives in the recoloured `.part-fit` tag. Full gate green (1083 tests, coverage 91.31%/82.18%/92.79%/95.02%); no balance harness run (pure chrome, zero sim surface). See `docs/sprints/sprint65.md`'s Exit for full detail.

A fifth maintainer playtest on 2026-07-15 (against the committed Sprint 65 build, i.e. the whole pass-2 arc; raw notes in `docs/playtest_notes/playtest-notes-2026-07-15.md`) produced 25 items, triaged and DESIGNED (not yet implemented) as **Sprints 66-69** (fewer, larger sprints per the maintainer's own framing). The two real system findings, both diagnosed against the code rather than taken at face value: (1) **the repair margin is exactly 20% of spend, by construction** - `repairStepFraction` makes a repair's cash cost and its bill reduction identical, so `marketRepairDiscount` (1.2) is the entire return; Law 1 holds (profit can never FALL, contrary to the note's wording) but 1.2 is far too thin to feel like the core loop, and it cannot be raised alone because `marketRepairDiscount x partsGeneration.maxBillFraction` must stay under 1 or a wrecked car's value goes negative into the scrap floor - a joint (D, F) decision, which is exactly why 1.2 was picked. (2) **condition is decoupled from mileage**: `upkeepTier` applies an ABSOLUTE baseline offset/jitter independent of age, so an 11 km car legitimately rolls poor parts, and `provenanceNote` is keyed only on upkeep tier, hence "service history unknown" on a nearly-new car. Sprint 66 (the honest car) takes both plus auction churn, proposing a new economy-bible **Law 6, the wage law** (a day at the bench must out-earn a day of rent, machine-checked in the coherence table); 67 (the work order II) fixes the car page (the inline cost going stale past one rung, one `rowCategory` line that causes BOTH the filter bug and the NA forced-induction-under-Missing bug, constant component order - explicitly retiring Sprint 41 decision 4 - a readable radar, a labour card, and a shared task list that an in-transit job finally shows); 68 (provenance and closure) fixes the real part-provenance bug by reusing Sprint 61's `baselineInstalledPartIds` (which already records exactly which instance was the customer's - `resolveRemovePart` tags by where the car is PARKED, not where the part came from, and close-out then confiscates the player's own mis-bought part), plus End Day warnings, reject-offer, an offer badge, and a sale receipt; 69 (the standing pass) adds real progress bars to the Standing screen and lengthens the reputation ladder, plus the small chrome cuts. **Item 24 exposed a real defect, since fixed ahead of the sprints (commit `8d6a8ee`): the Sprint 62 `/standing` screen was effectively UNREACHABLE** - the route was registered, but both entry links were styled `color: inherit; text-decoration: none` with a `--mg-panel-edge` dotted border, i.e. invisible on a dark panel, so nothing on screen was clickable. Sprint 62's Exit claiming it was "reached diegetically" was wrong; a DOM-reachable screen is not a reachable screen. Fixed with a real Standing nav entry, visible link styling, and an `App.test.ts` test pinning that the nav link resolves to `/standing` AND that the route renders. Three recorded decisions are required from the maintainer and are called out in the notes and the sprint docs: item 16 reverses Sprint 41 decision 4 (worst-band-first ordering); item 24 needs a SECOND progression-bible Law 4 amendment (Sprint 62's amendment allowed exact numbers but explicitly kept "no bar, no percentage", and the maintainer has now asked for bars); and the rep ladder rises ~4x across the board (local 15 -> 60, known 50 -> 200, respected 120 -> 500, legend 300 -> 1400), calibrated against the maintainer's OWN session rather than the harness. That recalibration exposed a significant finding, now in `TODO.md`: **the maintainer reached 32 rep and the `local` rung by day 6 (~5 rep/day) while the `competent-policy` probe earns ~1 rep/day and takes until p50 day 16** - so the hard-gated days-to-`local` invariant, the flagship progression-pacing check since Sprint 23, has been measuring bot patience rather than game pacing all along, and the old ladder was scaled to the bot (the old `local` at 15 falls on day 3 in real play). The maintainer explicitly overruled the resulting gate collision ("I don't care, just raise the requirements across the board"), which is the recorded approval the Sprint 29 precedent needs; the band gets re-based against the new ladder with the real figure disclosed, never force-passed. Also found while designing 69: `REPUTATION_TIER_THRESHOLDS` lives in `sim/constants.ts`, breaking the content law - the ladder moves to `economy.json` as part of that sprint so it is dialable without a code change. The maintainer's follow-up after seeing the Standing screen CANCELLED the first draft's proposal to move it onto Upgrades ("the standing page is fine, don't move it") - it stays at `/standing`. Implementation is gated on per-sprint maintainer approval per the standing cadence; 66 first (the value law moves every downstream number), the rest are order-independent.

**Sprint 66 (the honest car: the wage law, diminishing returns, coherent generation, real churn) is now implemented and committed**, adding economy-bible **Law 6** and amending **Laws 1 and 5**, all by explicit maintainer approval. Decisions 1-6 as designed: `marketRepairDiscount` 1.2 -> 1.5 paired with `partsGeneration.maxBillFraction` 0.7 -> 0.6 (the (D, F) interlock, now recorded as a law-level constraint in Law 2 so no future sprint moves one without the other); a `wearExposureByMileageKm` curve scaling the upkeep offset/jitter by the car's own mileage (item 6a fixed at the root: minimum generated mileage 11 km -> 8,503 km, the lowest-mileage car's worst band poor -> fine/worn); `(ageBand, upkeepTier)`-keyed provenance so a nearly-new car is never a barn find; `AUCTION_MIN_AGE_YEARS` (3); and ~doubled spawn rates (~3.45 lots/day). **Decision 7 (diminishing returns, keyed to tier) was added mid-sprint on the maintainer's direction and is the sprint's centre of gravity.** The first coherence read showed a mint restoration returning 1.10x rent on a kei against 7.34x on a rare car; my write-up filed that as a disclosure ("repair labour is value-blind"), and the maintainer's correction was the real finding: nobody should take a Honda City to mint in the first place - the model was fine, the TARGET BAND was the mistake, and the table was faithfully measuring an act no sane player performs. So `valuation.expectationByTier` (band + `beyondDiscount` + `aftermarketReturn` per tier) now splits the existing mint-referenced bill at each tier's market expectation band and discounts the halves at different rates: `base = clean - marketRepairDiscount x billBelow - beyondDiscount x billAbove`. Law 1 is SCOPED, not weakened - its >= 1 guarantee is absolute below the band (every repair the economy asks for), while above it `beyondDiscount` may fall under 1 deliberately, with a **legibility clause written into the law itself** (work above the band must be marked as passion spend on the car page; a value trap is one you cannot see) shipped as `passionSpendNotice`/`.passion-notice`, muted rather than alarming since it is a legitimate choice. Law 5 takes the same idea on the premium term via `aftermarketReturn`. `costToBandYen`/`carCostToBandYen` generalise the existing cost atoms and `costToMintYen`/`carCostToMintYen` now delegate to them, so the split can never drift from the bill the player sees. The result, machine-checked: buying rough and repairing to the expectation band clears **+25.4%** of clean value on a Honda City where chasing mint clears **+2.4%**, and the ordering reverses on the rare tier (Supra +34.5% sensible against +36.8% mint, so the concours push pays) - both directions are permanent assertions. The coherence table gains a `sensibleFlipMarginYen` headline plus the Law 6 wage row, and `balance.cli check` now hard-gates **11** invariants (up from 9). Full gate green (1096 tests, coverage 91.42/82.26/92.88/95.06); harness re-run, all 11 gates pass, days-to-`local` p50=16.0 unchanged from Sprint 61. Two golden hashes re-pinned (directive 17 case (a), with the justification recorded inline) and Sprint 59's unimproved-flip band re-measured 7% -> 8% (shitbox +7.3%, common +5.9%, uncommon +5.1%, rare +5.2%), disclosed rather than tuned away. **Honestly disclosed and deliberately not chased: every bot's day-100 cash went sharply negative** (Flipper -Y106,183 against Y300,000 starting cash). `bots/bandHelpers.ts` hardcodes `targetBand: 'mint'` and every strategy's done-check is `isGroupAtLeast(car, id, 'mint', ...)` - verified, not assumed - so every bot now executes the exact play decision 7 exists to punish, while the bot-free coherence table proves the same cars clear +9.6% to +34.5% on the sensible play. That gap is the clearest measurement yet of the known bot-harness defect (`TODO.md` finding 5); tuning the economy to make mint-blind bots solvent would tune away the sprint's whole point. Also deferred to `TODO.md`: the provenance pool stayed in `auctions.ts` rather than moving to `provenance.json`, the one piece of the sprint plan that did not land (a content-law violation on one constant). See `docs/sprints/sprint66.md`'s Exit for the full per-model table and the three wrong conclusions of mine the tests caught.

**Sprint 67 (the work order II: an accurate plan and a readable car page) is now implemented and committed** - all eight playtest items (7, 8, 9, 10, 12, 13, 16, 18), pure UI plus one derived store selector, `packages/sim/` untouched (the sprint's own check: no golden hash could move, and none did). Item 7's stale inline cost is fixed by a new `plannedStepFor(carId, componentId, carPartId?)` making the SAME `planGroupRepair` call the per-car sums already make, scoped to one address - so a row's caption is now the ROW's own planned total (what Confirm charges for it, summing to Confirm's figure by construction) and the next rung's increment moved entirely into the `+` button's tooltip; every number on screen answers exactly one question. **Items 18 and 10 were one line, as the discovery predicted**: `rowCategory` returned `null` for a legitimately-absent slot, which made the row unfilterable AND parked it under Missing; returning `'absent'` closes both and makes the filter's contract total (a row shows iff its category is ticked). Plus Show all / Hide all / Expand all / Collapse all (items 18/9); a shared `ServiceTaskList.vue` replacing three drifted inline lists and, with the `v-if="!job.inTransit"` guard dropped on both screens, an in-transit job finally shows what the customer asked for (item 12 - the half Sprint 61's inbound-car fit filter was missing); a first-class Labour tile on the garage and a promoted car-page card (item 13); and a readable radar (item 8: four concentric rings, one spoke per axis, and labels anchored by side so a long one grows away from the plot instead of riding into it). **Sprint 41 decision 4 is explicitly RETIRED** (item 16): `orderedComponents` drops the band sort for one constant order on every car, forever - its "surface the roughest work first" rationale is real but loses to muscle memory, and a test now wrecks the last group in declared order and asserts it stays last. Found and fixed while working, not deferred: **the Sprint 63 spelling guard had a hole and a real American spelling was in shipped copy** - `BANNED` held bare words, so `\blabor\b` could never match "labored", and the Components hint had read "priced and labored for real" since Sprint 63 with the guard green throughout; `BANNED` now enumerates inflections (deliberately not `\w*`, which trips on "laboratory") and **the guard checks itself** (+23 tests), since a guard that matches nothing passes exactly as quietly as a clean codebase - directly downstream of the 2026-07-15 testing conversation. Also fixed: a latent bug where a row planned to mint showed no cost at all (the caption shared the `+` button's `v-if`), and a vacuous assertion in my own new test (`.part-row` vs the real `.sub-part-row`). Full gate green (1134 tests, coverage 91.45/82.40/92.96/95.09). Flagged for the playtest rather than deviated from: decision 2 puts an NA car's forced-induction slot behind `Show all` by default, so the NA-to-turbo path is invisible until a player presses it (cheap fix if it reads badly: add `absent` to the default set). See `docs/sprints/sprint67.md`'s Exit for full detail.

**Sprint 68 (provenance and closure: whose part, which offer, what did I make) is now implemented and committed** - items 17, 11, 21, 22, 23. **Item 17 was a real bug and the fix was one line, using state that already existed:** `resolveRemovePart` decided the `customerJobId` tag by where the car was PARKED (tagging unconditionally in the service-job branch), so a part the player BOUGHT and fitted became the customer's property the instant they pulled it back off - and `resolveServiceJob`'s close-out, which drops every inventory entry carrying the job's id, then confiscated it outright. Sprint 61's `baselineInstalledPartIds` was already the exact missing signal (it records the `PartInstance.id` in each install-task slot at generation, and was only ever read by `isServiceTaskDone`); the new `isCustomersOwnPart` asks it the same question one slot over, so only the customer's original ever carries the tag. A legacy job with an empty baseline keeps the old tag-everything behaviour, since an empty baseline means "we cannot know whose this is" and the conservative read preserves the meaning an in-flight save was written under. Proved end to end (buy, fit, remove, close out, still owned with `pricePaidYen` intact), not as two resolvers checked apart. Also: `resolveRejectOffer` + an `offer-rejected` log entry (item 21 - no reputation cost, and `carsForSale` untouched so the car stays up for tomorrow's draw); an `offer today` badge on `ShopSlot` (item 22); a `SaleCompleteModal` mirroring `JobCompleteModal`'s store-ref + global-mount + Escape-priority shape, rendering what the Sprint 42 ledger and `car-sold` already knew and never showed (item 23 - a null profit renders as a dash when the purchase price was never known, never a fabricated number); and the End Day confirm reworked into a stacked warning list (item 11 - cart, a finished job nobody handed back, and unconfirmed planned work; warns, never blocks, and keeps every Sprint 51 hook name so App.vue's Escape handler needed no change). **No save-schema change, confirmed rather than assumed**: `offer-rejected` is a `DayLogEntry` and `dayLog` is a store ref `saveCodec.ts` never touches; `hasOffer` is derived at read time. `SAVE_VERSION` stays 30. Full gate green (1154 tests, coverage 91.42/81.63/92.81/95.01); no harness run and **no golden hash moved**, the expected result rather than a lucky one (no bot has ever mis-fitted a part onto a customer's car). Found while working: adding `offer-rejected` broke `dayLogFormat.ts`'s exhaustive switch, which is the type system correctly reporting that the day report had no idea how to render the new entry; and a test of my own was **vacuous** (an `if (!workDone) return` guard that always fired - measured, then fixed to force the state deterministically), the same class of miss as Sprint 67's `.part-row` selector. **Same-day follow-up, and the reason directive 19 exists:** the maintainer questioned an unrelated line ("how can a purchase price be unknown?"), which produced directive 19 (no save backwards compatibility before launch), whose review then caught that **Sprint 68's first pass had traded one theft for its mirror image**. `baselineInstalledPartIds` only recorded INSTALL-task slots, and the new ownership check read "no baseline for this slot" as "the player fitted it" - so on a job carrying any install task, the player could pull the customer's ENGINE (a slot no task touches) and keep it, with a test asserting the wrong behaviour and confident wrong reasoning attached. Fixed at the source rather than patched at the reader: the baseline is now TOTAL over the car (every slot snapshots what arrived, `null` for a slot that arrived empty - which no real `PartInstance.id` can equal, so a part fitted into an empty slot is decided by the record rather than by the absence of one), and `isCustomersOwnPart` collapses to one identity check. Directive 19 paid for itself immediately: the legacy empty-baseline branch is DELETED rather than preserved, and it turned out to be quietly load-bearing for repair-only jobs (whose baseline is empty) - exactly the "dead semantics alive in live logic" the directive names. No schema change (the Zod record already permits any keys) and no golden hash moved (the scripted career never accepts a service job). See `docs/sprints/sprint68.md`'s Exit for full detail.

Full sprint-by-sprint detail lives one file per sprint in `docs/sprints/sprintNN.md`; read the current sprint's doc (and the previous one, per the Sprint workflow above) before starting new work, not this paragraph.

- `docs/design/midnight-garage-gdd.md` - Game Design Document (v0.6). Vision, mechanics, economy, art direction, tech stack. The GDD feature set is **frozen for v1.0**; new ideas go to `IDEAS.md` (repo root, tracked), not into the GDD. `IDEAS.md` is the post-launch parking lot - anything there is out of v1.0 scope by definition. Note: it records a maintainer-sanctioned *post-launch* exception to two hard design rules below (an optional, zero-weight driving minigame) - so don't flag that idea as a rules violation; it's an accepted, deliberately-deferred opt-in, not an oversight.
- `docs/design/midnight-garage-roadmap.md` - Sprint-by-sprint plan (P0–P7), risk register, engineering standards, content pipeline.
- `docs/design/midnight-garage-roster.md` - Full car roster with scope tiers (PoC-10 → Go-Live ~48 → expansion packs). **Contains a secret easter egg ("The Zero Legend") - never surface it in public-facing text, devlogs, or marketing copy.**
- `docs/design/art-direction.md` - The art bible (locked 2026-07-13): palette tiers ("sodium on tarmac"), pixel discipline (640x360 stage, 96x48 car masters, integer-only scaling), the diegetic/visceral-UI law (every control is an in-world object with a feedback stack - binding on all UI work), and the audio direction (diegetic shop radio, sourcing license rules). Canonical for look-and-feel; GDD stays canonical for mechanics. No AI-generated assets ship or appear in public materials, ever.
- `docs/design/progression-bible.md` - Canonical rules for reputation/specialty/tool-tier progression (locked 2026-07-12): the four pillars, the six laws, banned vocabulary. Amendments require explicit maintainer approval recorded in the doc.
- `docs/design/economy-bible.md` - Canonical rules for car value, repair cost, and parts pricing (locked 2026-07-14, Economy Rebuild arc): the four laws (repair margin, no value traps, proportionate parts, one derived ledger), the playtest diagnosis that produced them, the centralised pricing-sheet formula, and the anchor inventory. Amendments require explicit maintainer approval recorded in the doc.

When design questions arise, these documents are the source of truth. Don't invent mechanics or cars - check the docs, and treat conflicts between them as something to flag to the user (GDD is canonical for mechanics, roster for car scope).

## Locked technical decisions (GDD §13, roadmap §5)

The stack is decided; do not re-litigate it when scaffolding begins:

- TypeScript (strict) • Vue 3 Composition API • Pinia • Vue Router (memory-history mode; added Sprint 04) • Vite • PixiJS v8 (canvas islands only) • Dexie.js/IndexedDB saves • Zod-validated JSON content • Howler.js audio • Vitest + @vue/test-utils + happy-dom for component tests (the latter two added Sprint 04) • static deploy (Cloudflare Pages / itch.io). Python (polars) for the balance harness. pnpm monorepo.

Planned layout: `packages/sim` (pure TS sim core), `packages/game` (Vue app), `packages/content` (JSON + Zod schemas), `tools/balance` (Python CLI).

Engineering laws that all future code must obey:

1. **Boundary law:** `packages/sim` never imports Vue/Pixi/DOM/Dexie/`@midnight-garage/game` - enforced via ESLint import rules. It does depend on `@midnight-garage/content` (pure data + Zod), never the reverse - that's the one dependency the boundary law permits. Sim contract: `advanceDay(state, queuedActions, seed) → newState + eventLog`, fully deterministic (seeded PRNG; no `Date.now()`/`Math.random()` in sim) - implemented in `packages/sim/src/advanceDay.ts` from Sprint 02 onward.
2. **Content law:** any designer-tunable number lives in JSON under `packages/content`, not in code.
3. **Naming Layer (licensing):** every vehicle separates real `spec` data from swappable `display_name`/`brand` strings; a single config flag flips the whole game to parody names, with a CI test asserting no real-brand string leaks when flagged. Built from the first schema onward. Parts brands are parody-only from day one.
4. **Save law:** **SUSPENDED until launch by directive 19 (2026-07-15) - a schema change is now just a Dexie version bump; no migration, no golden-save test, no legacy-compat branch.** The pre-launch cost was real and bought nothing: there are no players and no old saves. The law as written, in force again the moment the game reaches real players: every save-schema change = Dexie version bump + migration + golden-save test in the same PR. Unaffected either way: call `navigator.storage.persist()` and keep the export-save-string flow prominent (Safari ITP can evict IndexedDB).
5. **Test law:** golden-master seed tests in CI; every fixed sim bug gets a regression test; balance invariants (bot careers → CSV → polars report, `pnpm balance:run` then `python -m balance.cli check`) exist and pass as of Sprint 03. **Wired into CI as of 2026-07-09** (external review 2026-07 finding 1, revisiting the Sprint-03 local-only deferral): a path-filtered `balance` job (`packages/sim/**` / `packages/content/data/**`) runs the full 1000-career-per-strategy harness + invariant check + uploads `report.md` as a build artifact, skipped (not failed) on pushes/PRs that don't touch sim or content data. See `.github/workflows/ci.yml`. Coverage is measured and gated too (`pnpm test:coverage`, added 2026-07-09) - see Commands below.
6. **Pre-commit/pre-push hooks (added 2026-07-09):** Husky + lint-staged. `pre-commit` runs `lint-staged` (ESLint --fix + Prettier --write on staged files only - fast, auto-fixes and re-stages). `pre-push` runs the full local gate (`typecheck` → `lint` → `format` → `test:coverage`) so a broken push is caught before it reaches CI, not just by it. Neither hook runs `build` (CI still does) - kept out for speed, since typecheck/lint/test already catch the overwhelming majority of real breakage.

## Hard design rules (accessibility & scope)

- **No reflex-based input anywhere** - no QTEs, no timing bars. Everything is decision-paced.
- No real-time waiting, energy systems, or monetized timers. Turn-based days: nothing happens while the browser is closed.
- No driving gameplay - events resolve via pre-run decisions + animated resolution.
- Currency is era-authentic yen (¥), everywhere.

## Commands

Run from the repo root (pnpm workspace):

- `pnpm install` - install all workspace dependencies
- `pnpm typecheck` - `tsc --noEmit` in sim/content, `vue-tsc --noEmit` in game
- `pnpm lint` - ESLint (includes the sim boundary-law rule)
- `pnpm format` / `pnpm format:fix` - Prettier check / write
- `pnpm test` - Vitest across all workspace projects; single project: `pnpm test --project sim`; single file: `pnpm test packages/sim/tests/rng.test.ts`
- `pnpm test:coverage` - Vitest with v8 coverage (text/html/lcov reports to `coverage/`, gitignored). Thresholds enforced (statements 80 / branches 65 / functions 78 / lines 82, set 2026-07-09 as a ratchet against the real measured baseline - raise them as coverage improves, don't lower them to pass). Excludes Pixi rendering, the art-spike/dev-sandbox screens, the dev-only console, and `saveDb.ts` (deliberately a thin Dexie wrapper, untested by design per Sprint 07) - everything else counts. Run in CI and in the pre-push hook.
- `pnpm build` - production build of `packages/game` (Vite)
- `pnpm dev` - game dev server (long-running: give this to the user to run, never run it yourself)
- `pnpm balance:run` - plays all 9 bot strategies (8 archetypes plus `competent-policy`, Sprint 23's measurement probe for the reputation-pacing invariant) through 1,000 seeded 100-day careers each and writes `tools/balance/data/careers.csv` + `careers.manifest.json` (also carrying `startingCashYen`/`weeklyRentYen` since Sprint 23, so the Python check validates against the values that actually ran) plus `auctionWins.csv`/`auctionFieldSizes.csv` for the auction-calibration section (since Sprint 10) and `acquisitions.csv` for the buyout-vs-bid telemetry section (since 2026-07-09). Builds the CLI via a dedicated `packages/sim/tsconfig.cli.json` (CommonJS, run with plain `node`) first, then two post-build fixups: `scripts/mark-commonjs.cjs` (marks the dist subtree CommonJS) and `scripts/fixContentRequires.cjs` (rewrites compiled `require("@midnight-garage/content")` calls to the dist-local relative path - `@midnight-garage/content`'s `package.json` `exports` points at raw `src/index.ts`, which plain Node can't resolve, so `tsconfig.cli.json` compiles it as an explicit root and this script points the requires at the result). **Wired into CI as of 2026-07-09** (path-filtered `balance` job) - still fine to run locally whenever you want fresher numbers than the last CI run.
- `python -m balance.cli report --data-dir tools/balance/data --out tools/balance/report.md` - renders the markdown balance report (median/p10/p90 cash + car count at day 25/40/70/100 per strategy, the auction win-price calibration, and the buyout-vs-bid share per strategy). Requires `pip install -e tools/balance` once (polars is the only dependency).
- `python -m balance.cli check --data-dir tools/balance/data` - checks 6 balance invariants against the CSV (Sprint 23 decision 7: 3 hard-gated - days-to-`local` pacing, buyout share, the 3 legacy Sprint 03/09 checks - 3 informational with real numbers disclosed rather than force-passed, see `invariants.py`'s module docstring and `sprint23.md`'s Exit); exits non-zero on any hard-gated failure.

CI (`.github/workflows/ci.yml`) runs typecheck, lint, format, coverage-gated test, build on every push/PR (`check` job) and deploys `main` to Cloudflare Pages once the secrets exist (`deploy` job). A third, path-filtered `balance` job (`packages/sim/**`/`packages/content/data/**`) runs the balance harness + invariant check on pushes/PRs that touch sim or content data, uploading `report.md` as an artifact - wired 2026-07-09, revisiting the Sprint-03 local-only deferral per external review finding 1 (see `docs/sprints/sprint03.md`). `deploy` waits on both `check` and `balance` (skipped counts as passing when nothing sim/content-relevant changed).
